---
sidebar_position: 8
title: Graph Detective
---

# Graph Detective

改代码，按运行，让实线对上虚线。 


<br />
<ExerciseSet>

<Exercise title="linear(x)">
<GraphChallenge
  title="linear(x)"
  hint="a=(0, 1); b=(-1, 0)"
  functionName="linear"
  starter={`def linear(x):
    return x - 1
`}
  targetBody={`    return x + 1`}
  xMin={-6}
  xMax={6}
  yMin={-6}
  yMax={8}
/>
</Exercise>

<Exercise title="linear_b(x)">
<GraphChallenge
  title="linear_b(x)"
  hint="a=(0, 3); b=(-3, 0)"
  functionName="linear_b"
  starter={`def linear_b(x):
    return x + 1
`}
  targetBody={`    return x + 3`}
  xMin={-6}
  xMax={6}
  yMin={-4}
  yMax={10}
/>
</Exercise>

<Exercise title="doubler(x)">
<GraphChallenge
  title="doubler(x)"
  hint="a=(0, 0); b=(2, 4); c=(-2, -4)"
  functionName="doubler"
  starter={`def doubler(x):
    return x + 2
`}
  targetBody={`    return 2 * x`}
  xMin={-4}
  xMax={4}
  yMin={-8}
  yMax={8}
/>
</Exercise>

<Exercise title="tripler(x)">
<GraphChallenge
  title="tripler(x)"
  hint="a=(0, 0); b=(2, 6); c=(-2, -6)"
  functionName="tripler"
  starter={`def tripler(x):
    return 2 * x
`}
  targetBody={`    return 3 * x`}
  xMin={-4}
  xMax={4}
  yMin={-12}
  yMax={12}
/>
</Exercise>

<Exercise title="square(x)">
<GraphChallenge
  title="square(x)"
  hint="a=(0, 0); b=(-2, 4); c=(2, 4)"
  functionName="square"
  starter={`def square(x):
    return x * 2
`}
  targetBody={`    return x ** 2`}
  xMin={-4}
  xMax={4}
  yMin={0}
  yMax={16}
/>
</Exercise>

<Exercise title="square_up(x)">
<GraphChallenge
  title="square_up(x)"
  hint="a=(0, 2); b=(-2, 6); c=(2, 6)"
  functionName="square_up"
  starter={`def square_up(x):
    return x ** 2
`}
  targetBody={`    return x ** 2 + 2`}
  xMin={-4}
  xMax={4}
  yMin={0}
  yMax={18}
/>
</Exercise>

<Exercise title="distance(x)">
<GraphChallenge
  title="distance(x)"
  hint="a=(0, 0); b=(-3, 3); c=(3, 3)"
  functionName="distance"
  starter={`def distance(x):
    return x
`}
  targetBody={`    return abs(x)`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={7}
/>
</Exercise>

<Exercise title="distance_up(x)">
<GraphChallenge
  title="distance_up(x)"
  hint="a=(0, 2); b=(-3, 5); c=(3, 5)"
  functionName="distance_up"
  starter={`def distance_up(x):
    return abs(x)
`}
  targetBody={`    return abs(x) + 2`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={8}
/>
</Exercise>

<Exercise title="shifted(x)">
<GraphChallenge
  title="shifted(x)"
  hint="a=(0, 3); b=(-3, 0)"
  functionName="shifted"
  starter={`def shifted(x):
    return x
`}
  targetBody={`    return x + 3`}
  xMin={-6}
  xMax={6}
  yMin={-4}
  yMax={10}
/>
</Exercise>

<Exercise title="shifted_more(x)">
<GraphChallenge
  title="shifted_more(x)"
  hint="a=(0, 5); b=(-5, 0)"
  functionName="shifted_more"
  starter={`def shifted_more(x):
    return x + 3
`}
  targetBody={`    return x + 5`}
  xMin={-6}
  xMax={6}
  yMin={-2}
  yMax={12}
/>
</Exercise>

<Exercise title="flipped(x)">
<GraphChallenge
  title="flipped(x)"
  hint="a=(0, 0); b=(2, -2); c=(-2, 2)"
  functionName="flipped"
  starter={`def flipped(x):
    return x
`}
  targetBody={`    return -x`}
  xMin={-6}
  xMax={6}
  yMin={-6}
  yMax={6}
/>
</Exercise>

<Exercise title="flipped_up(x)">
<GraphChallenge
  title="flipped_up(x)"
  hint="a=(0, 2); b=(2, 0); c=(-2, 4)"
  functionName="flipped_up"
  starter={`def flipped_up(x):
    return -x
`}
  targetBody={`    return -x + 2`}
  xMin={-6}
  xMax={6}
  yMin={-4}
  yMax={8}
/>
</Exercise>

<Exercise title="scaled(x)">
<GraphChallenge
  title="scaled(x)"
  hint="a=(0, 0); b=(1, 3); c=(2, 6)"
  functionName="scaled"
  starter={`def scaled(x):
    return x + 1
`}
  targetBody={`    return 3 * x`}
  xMin={-4}
  xMax={4}
  yMin={-12}
  yMax={12}
/>
</Exercise>

<Exercise title="scaled_strong(x)">
<GraphChallenge
  title="scaled_strong(x)"
  hint="a=(0, 0); b=(1, 5); c=(2, 10)"
  functionName="scaled_strong"
  starter={`def scaled_strong(x):
    return 3 * x
`}
  targetBody={`    return 5 * x`}
  xMin={-4}
  xMax={4}
  yMin={-20}
  yMax={20}
/>
</Exercise>

<Exercise title="combo(x)">
<GraphChallenge
  title="combo(x)"
  hint="a=(0, -1); b=(-1, 1); c=(1, 1)"
  functionName="combo"
  starter={`def combo(x):
    return x ** 2
`}
  targetBody={`    return 2 * x ** 2 - 1`}
  xMin={-3}
  xMax={3}
  yMin={-2}
  yMax={18}
/>
</Exercise>

<Exercise title="combo2(x)">
<GraphChallenge
  title="combo2(x)"
  hint="a=(0, 2); b=(-1, 5); c=(1, 5)"
  functionName="combo2"
  starter={`def combo2(x):
    return 2 * x ** 2 - 1
`}
  targetBody={`    return 3 * x ** 2 + 2`}
  xMin={-3}
  xMax={3}
  yMin={0}
  yMax={30}
/>
</Exercise>

<Exercise title="positive_part(x)">
<GraphChallenge
  title="positive_part(x)"
  hint="a=(-3, 0); b=(0, 0); c=(3, 3)"
  functionName="positive_part"
  starter={`def positive_part(x):
    return x
`}
  targetBody={`    return max(0, x)`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={7}
/>
</Exercise>

<Exercise title="positive_part_shift(x)">
<GraphChallenge
  title="positive_part_shift(x)"
  hint="a=(-1, 0); b=(2, 0); c=(5, 3)"
  functionName="positive_part_shift"
  starter={`def positive_part_shift(x):
    return max(0, x)
`}
  targetBody={`    return max(0, x - 2)`}
  xMin={-4}
  xMax={8}
  yMin={0}
  yMax={7}
/>
</Exercise>

<Exercise title="cap(x)">
<GraphChallenge
  title="cap(x)"
  hint="a=(0, 0); b=(4, 4); c=(6, 4)"
  functionName="cap"
  starter={`def cap(x):
    return x
`}
  targetBody={`    return min(x, 4)`}
  xMin={-2}
  xMax={8}
  yMin={-2}
  yMax={5}
/>
</Exercise>

<Exercise title="cap_loose(x)">
<GraphChallenge
  title="cap_loose(x)"
  hint="a=(0, 0); b=(6, 6); c=(8, 6)"
  functionName="cap_loose"
  starter={`def cap_loose(x):
    return min(x, 4)
`}
  targetBody={`    return min(x, 6)`}
  xMin={-2}
  xMax={10}
  yMin={-2}
  yMax={7}
/>
</Exercise>

<Exercise title="sign(x)">
<GraphChallenge
  title="sign(x)"
  hint="a=(-2, -1); b=(0, 0); c=(2, 1)"
  functionName="sign"
  starter={`def sign(x):
    return x
`}
  targetBody={`    return 1 if x > 0 else (-1 if x < 0 else 0)`}
  xMin={-4}
  xMax={4}
  yMin={-1}
  yMax={1}
/>
</Exercise>

<Exercise title="sign_tall(x)">
<GraphChallenge
  title="sign_tall(x)"
  hint="a=(-2, -2); b=(0, 0); c=(2, 2)"
  functionName="sign_tall"
  starter={`def sign_tall(x):
    return 1 if x > 0 else (-1 if x < 0 else 0)
`}
  targetBody={`    return 2 * (1 if x > 0 else (-1 if x < 0 else 0))`}
  xMin={-4}
  xMax={4}
  yMin={-2}
  yMax={2}
/>
</Exercise>

<Exercise title="sawtooth(x)">
<GraphChallenge
  title="sawtooth(x)"
  hint="a=(0, 0); b=(2, 2); c=(3, 0)"
  functionName="sawtooth"
  starter={`def sawtooth(x):
    return x
`}
  targetBody={`    return x % 3`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={3}
/>
</Exercise>

<Exercise title="sawtooth5(x)">
<GraphChallenge
  title="sawtooth5(x)"
  hint="a=(0, 0); b=(3, 3); c=(5, 0)"
  functionName="sawtooth5"
  starter={`def sawtooth5(x):
    return x % 3
`}
  targetBody={`    return x % 5`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={5}
/>
</Exercise>

<Exercise title="zigzag(x)">
<GraphChallenge
  title="zigzag(x)"
  hint="a=(0, 2); b=(2, 0); c=(4, 2)"
  functionName="zigzag"
  starter={`def zigzag(x):
    return abs(x)
`}
  targetBody={`    return abs(x % 4 - 2)`}
  xMin={-8}
  xMax={8}
  yMin={0}
  yMax={3}
/>
</Exercise>

<Exercise title="zigzag_wide(x)">
<GraphChallenge
  title="zigzag_wide(x)"
  hint="a=(0, 3); b=(3, 0); c=(6, 3)"
  functionName="zigzag_wide"
  starter={`def zigzag_wide(x):
    return abs(x % 4 - 2)
`}
  targetBody={`    return abs(x % 6 - 3)`}
  xMin={-9}
  xMax={9}
  yMin={0}
  yMax={4}
/>
</Exercise>

<Exercise title="parity(x)">
<GraphChallenge
  title="parity(x)"
  hint="a=(-2, 0); b=(-1, 1); c=(0, 0); d=(1, 1)"
  functionName="parity"
  starter={`def parity(x):
    return x
`}
  targetBody={`    return x % 2`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={1}
/>
</Exercise>

<Exercise title="parity3(x)">
<GraphChallenge
  title="parity3(x)"
  hint="a=(0, 0); b=(1, 1); c=(2, 2); d=(3, 0)"
  functionName="parity3"
  starter={`def parity3(x):
    return x % 2
`}
  targetBody={`    return x % 3`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={3}
/>
</Exercise>

<Exercise title="almost_square(x)">
<GraphChallenge
  title="almost_square(x)"
  hint="a=(-2, 4); b=(0, 0); c=(2, 4)"
  functionName="almost_square"
  starter={`def almost_square(x):
    return x * abs(x)
`}
  targetBody={`    return x ** 2`}
  xMin={-4}
  xMax={4}
  yMin={0}
  yMax={16}
/>
</Exercise>

<Exercise title="almost_distance(x)">
<GraphChallenge
  title="almost_distance(x)"
  hint="a=(0, 0); b=(-3, 3); c=(3, 3)"
  functionName="almost_distance"
  starter={`def almost_distance(x):
    return abs(x) + 1
`}
  targetBody={`    return abs(x)`}
  xMin={-6}
  xMax={6}
  yMin={0}
  yMax={7}
/>
</Exercise>

<Exercise title="almost_doubler(x)">
<GraphChallenge
  title="almost_doubler(x)"
  hint="a=(-1, 0); b=(0, 2); c=(2, 6)"
  functionName="almost_doubler"
  starter={`def almost_doubler(x):
    return 2 * x + 1
`}
  targetBody={`    return 2 * (x + 1)`}
  xMin={-4}
  xMax={4}
  yMin={-6}
  yMax={10}
/>
</Exercise>

</ExerciseSet>
