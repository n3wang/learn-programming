---
sidebar_position: 2
title: 🇯🇵 1a-II — Kaomoji Composer
---

# Kaomoji Composer

改代码，按运行，让输出对上左边。 Fix the code, press Run, match the target.

Part 2 of [Printing Japanese Emoticons](./project-1a).

<br />

<ExerciseSet>

<Exercise title="cat fish cat">
<OutputChallenge
  title="cat fish cat"
  target={`(≧◡≦)
(°△°)
(≧◡≦)`}
  starter={`cat = "(≧◡≦)"
fish = "(°△°)"
print(fish)
print(cat)
print(fish)
`}
/>
</Exercise>

<Exercise title="c a b">
<OutputChallenge
  title="c a b"
  target={`(★ω★)
(◕‿◕)
(⌒▽⌒)`}
  starter={`a = "(◕‿◕)"
b = "(⌒▽⌒)"
c = "(★ω★)"
print(a)
print(b)
print(c)
`}
/>
</Exercise>

<Exercise title="sun moon x2">
<OutputChallenge
  title="sun moon x2"
  target={`(＾▽＾)
(╥_╥)
(＾▽＾)
(╥_╥)`}
  starter={`sun = "(＾▽＾)"
moon = "(╥_╥)"
print(sun)
print(moon)
print(sun)
`}
/>
</Exercise>

<Exercise title="wake sleep wake">
<OutputChallenge
  title="wake sleep wake"
  target={`(･ω･)
(╥_╥)
(･ω･)`}
  starter={`wake = "(･ω･)"
sleep = "(╥_╥)"
print(sleep)
print(wake)
print(sleep)
`}
/>
</Exercise>

<Exercise title="each once">
<OutputChallenge
  title="each once"
  target={`(◕‿◕)
(⌒▽⌒)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use a for loop over the list'},
  ]}
  starter={`faces = ["(◕‿◕)", "(⌒▽⌒)"]
print(faces[0])
print(faces[1])
`}
/>
</Exercise>

<Exercise title="one face x3">
<OutputChallenge
  title="one face x3"
  target={`(°▽°)
(°▽°)
(°▽°)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use a for loop'},
    {name: 'range', pattern: 'range\\s*\\(', must: true, hint: 'use range(...) to repeat'},
  ]}
  starter={`faces = ["(°▽°)"]
print(faces[0])
print(faces[0])
print(faces[0])
`}
/>
</Exercise>

<Exercise title="reverse">
<OutputChallenge
  title="reverse"
  target={`(★ω★)
(⌒▽⌒)
(◕‿◕)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use a for loop'},
    {name: 'reverse', pattern: 'reversed|\\[::-1\\]', must: true, hint: 'loop the list in reverse'},
  ]}
  starter={`faces = ["(◕‿◕)", "(⌒▽⌒)", "(★ω★)"]
for face in faces:
    print(face)
`}
/>
</Exercise>

<Exercise title="each twice">
<OutputChallenge
  title="each twice"
  target={`(◕‿◕)
(◕‿◕)
(⌒▽⌒)
(⌒▽⌒)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use nested or repeated loops'},
    {name: 'range', pattern: 'range\\s*\\(', must: true, hint: 'use range(...) to repeat'},
  ]}
  starter={`faces = ["(◕‿◕)", "(⌒▽⌒)"]
for face in faces:
    print(face)
`}
/>
</Exercise>

<Exercise title="grow then repeat">
<OutputChallenge
  title="grow then repeat"
  target={`(◕‿◕)
(◕‿◕)
(◕‿◕)
(★ω★)
(◕‿◕)
(★ω★)`}
  sourceChecks={[
    {name: 'append', pattern: '\\.append\\s*\\(', must: true, hint: 'use list.append(...)'},
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use for loops'},
    {name: 'range', pattern: 'range\\s*\\(\\s*2\\s*\\)', must: true, hint: 'loop twice with range(2)'},
  ]}
  starter={`faces = ["(◕‿◕)"]
star = "(★ω★)"

for _ in range(2):
    for face in faces:
        print(face)
`}
  height="200px"
/>
</Exercise>

<Exercise title="add moon">
<OutputChallenge
  title="add moon"
  target={`(＾▽＾)
(＾▽＾)
(＾▽＾)
(╥_╥)
(＾▽＾)
(╥_╥)`}
  sourceChecks={[
    {name: 'append', pattern: '\\.append\\s*\\(', must: true, hint: 'use list.append(...)'},
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use for loops'},
    {name: 'range', pattern: 'range\\s*\\(\\s*2\\s*\\)', must: true, hint: 'loop twice with range(2)'},
  ]}
  starter={`faces = ["(＾▽＾)"]
moon = "(╥_╥)"

for _ in range(2):
    for face in faces:
        print(face)
`}
  height="200px"
/>
</Exercise>

<Exercise title="plus friend">
<OutputChallenge
  title="plus friend"
  target={`(°▽°)
(°▽°)
(°▽°)
(≧◡≦)
(°▽°)
(≧◡≦)`}
  sourceChecks={[
    {name: 'append', pattern: '\\.append\\s*\\(', must: true, hint: 'use list.append(...)'},
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use for loops'},
    {name: 'range', pattern: 'range\\s*\\(\\s*2\\s*\\)', must: true, hint: 'loop twice with range(2)'},
  ]}
  starter={`faces = ["(°▽°)"]
friend = "(≧◡≦)"

for _ in range(2):
    for face in faces:
        print(face)
`}
  height="200px"
/>
</Exercise>

<Exercise title="group a then b">
<OutputChallenge
  title="group a then b"
  target={`(^_^)
(>_<)
(◕‿◕)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use for loops'},
  ]}
  starter={`group_a = ["(^_^)", "(>_<)"]
group_b = ["(◕‿◕)"]
print(group_a[0])
print(group_a[1])
print(group_b[0])
`}
/>
</Exercise>

<Exercise title="morning then night">
<OutputChallenge
  title="morning then night"
  target={`(◕‿◕)
(⌒▽⌒)
(╥_╥)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use a for loop on each list'},
  ]}
  starter={`morning = ["(◕‿◕)", "(⌒▽⌒)"]
night = ["(╥_╥)"]
for face in morning:
    print(face)
print(night[0])
`}
/>
</Exercise>

<Exercise title="two loops">
<OutputChallenge
  title="two loops"
  target={`(★ω★)
(＾▽＾)
(°▽°)
(╥_╥)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use two for loops'},
  ]}
  starter={`bright = ["(★ω★)", "(＾▽＾)"]
dim = ["(°▽°)", "(╥_╥)"]
for face in bright:
    print(face)
print(dim[0])
print(dim[1])
`}
/>
</Exercise>

<Exercise title="each list x2">
<OutputChallenge
  title="each list x2"
  target={`(◕‿◕)
(◕‿◕)
(⌒▽⌒)
(⌒▽⌒)
(╥_╥)
(╥_╥)`}
  sourceChecks={[
    {name: 'for loop', pattern: '\\bfor\\b', must: true, hint: 'use for loops on both lists'},
    {name: 'range', pattern: 'range\\s*\\(', must: true, hint: 'repeat with range(...)'},
  ]}
  starter={`happy = ["(◕‿◕)", "(⌒▽⌒)"]
sad = ["(╥_╥)"]
for face in happy:
    print(face)
for face in sad:
    print(face)
`}
  height="180px"
/>
</Exercise>

</ExerciseSet>
