---
sidebar_position: 2
title: Chapter 1a - Printing
---

## Printing Messages

```python
print("Hello World!")
print('Hello World!')
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="220px"
  code={`print("Hello World!")
print('Hello World!')
`}
/>

</details>

## Mini quiz — print

<MultipleChoice
  id="py-1a-print"
  title="print"
  questions={[
    {
      prompt: 'What does this show?',
      code: 'print("Hello")',
      codeLang: 'python',
      choices: ['Hello', '"Hello"', 'print', 'Error'],
      answer: 0,
      why: 'Quotes are for Python, not for the screen.',
    },
    {
      prompt: 'What does this print?',
      code: 'print(4 * 3)',
      codeLang: 'python',
      choices: ['4*3', '12', '43', '7'],
      answer: 1,
      why: 'The expression is calculated first.',
    },
  ]}
/>

:::important Japanese Emoticons project
[Open project 1a](../project/project-1a)
:::

## Printing numbers

`print(4 * 3)` prints **12**, not the text `4*3`.

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="240px"
  code={`print("Welcome to Python Programming")
print("Do you know what is 4*3?")
print(4 * 3)
`}
/>

</details>

## Challenge: print a product

Write `product()` so it **prints** the result of `6 * 7` (one number, nothing else).

<CodingExam
  title="print a product"
  heading="Challenge: print a product"
  lang="python"
  filename="main.py"
  prompt="Print 6 * 7. Hidden tests check the output is 42."
  sampleLog={`42`}
  starter={`def product():
    # TODO: print 6 * 7
    print(0)
`}
  wrapSuffix={`
product()
`}
  tests={[
    { name: 'prints 42', stdin: '', equals: '42' },
  ]}
/>

## Special characters

`\n` starts a new line. `\\` prints one backslash.

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="240px"
  code={`print("Hello\\nWorld")
print("\\\\")
`}
/>

</details>

## Mini quiz — escape characters

<MultipleChoice
  id="py-1a-escape"
  title="Escape characters"
  questions={[
    {
      prompt: 'What does \\n do inside a string?',
      choices: ['Prints n', 'Starts a new line', 'Deletes a line', 'Nothing'],
      answer: 1,
      why: 'It is the newline character.',
    },
    {
      prompt: 'How do you print one backslash?',
      code: `print('\\\\')
print('\\')`,
      codeLang: 'python',
      choices: ["the first line", "the second line", 'print(backslash)', 'print(/)'],
      answer: 0,
      why: 'The first backslash escapes the second.',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Two lines" anchor="exercise-two-lines">

:::tip Activity: Two lines
Write `greet()` so it prints `Hi` on the first line and `Ada` on the second line.

```
Hi
Ada
```

<CodingExam
  title="Two lines"
  heading="exercise-two-lines"
  lang="python"
  filename="main.py"
  prompt="Print Hi then Ada on two lines."
  sampleLog={`Hi
Ada`}
  starter={`def greet():
    # TODO: two print calls
    pass
`}
  wrapSuffix={`
greet()
`}
  tests={[
    { name: 'Hi then Ada', stdin: '', equals: 'Hi\nAda' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
