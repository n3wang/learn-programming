---
sidebar_position: 9
title: Chapter 4b - Conditionals
---

`if` runs a block only when the test is true. `else` runs when it is not.

```python
if n == 1:
    print("Outside is hot")
else:
    print("not hot")
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="260px"
  code={`n = 1
if n == 1:
    print("Outside is hot")
else:
    print("not hot")
`}
/>

</details>

## Mini quiz — if and else

<MultipleChoice
  id="py-4b-if-else"
  title="if and else"
  questions={[
    {
      prompt: 'If n is 1, which branch runs?',
      code: `if n == 1:
    print("Outside is hot")
else:
    print("not hot")`,
      codeLang: 'python',
      choices: ['only else', 'the if body', 'both', 'neither'],
      answer: 1,
      why: 'The test n == 1 is true.',
    },
    {
      prompt: 'If n is 0, which branch runs?',
      code: `if n == 1:
    print("Outside is hot")
else:
    print("not hot")`,
      codeLang: 'python',
      choices: ['the if body', 'the else body', 'both', 'the program crashes'],
      answer: 1,
      why: 'The test is false, so else runs.',
    },
  ]}
/>

:::important Zodiac project
[Project 4b](/python/project/project-4b)
:::

## Challenge: weather line

Write `weather(n)` so it returns `"hot"` if `n` is `1`, otherwise `"cool"`.

<CodeExercise
  title="weather line"
  heading="Challenge: weather line"
  lang="python"
  filename="main.py"
  prompt="Return hot if n is 1, else cool."
  sampleLog={`(input) 1
hot`}
  starter={`def weather(n):
    # TODO
    return ""
`}
  wrapSuffix={`
print(weather(int(input())))
`}
  tests={[
    { name: '1 → hot', stdin: '1', equals: 'hot' },
    { name: '0 → cool', stdin: '0', equals: 'cool' },
    { name: '2 → cool', stdin: '2', equals: 'cool' },
  ]}
/>

## Even and odd

`n % 2 == 0` means even.

## Mini quiz — if

<MultipleChoice
  id="py-4b-if"
  title="if"
  questions={[
    {
      prompt: 'What does else do?',
      choices: ['Repeats the if', 'Runs when the if test is false', 'Always runs', 'Stops the program'],
      answer: 1,
      why: 'else is the other branch.',
    },
    {
      prompt: 'Which test is true for even numbers?',
      code: 'n % 2 == 0',
      codeLang: 'python',
      choices: ['n % 2 == 1', 'n % 2 == 0', 'n / 2', 'n == even'],
      answer: 1,
      why: 'Remainder 0 means divisible by 2.',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Even or odd" anchor="exercise-even-or-odd">

:::tip Activity: Even or odd
Write `label(n)` so it returns `even` or `odd`.

<CodeExercise
  title="Even or odd"
  heading="exercise-even-or-odd"
  lang="python"
  filename="main.py"
  prompt="Return even or odd."
  sampleLog={`(input) 4
even`}
  starter={`def label(n):
    # TODO
    return ""
`}
  wrapSuffix={`
print(label(int(input())))
`}
  tests={[
    { name: '4 even', stdin: '4', equals: 'even' },
    { name: '7 odd', stdin: '7', equals: 'odd' },
    { name: '0 even', stdin: '0', equals: 'even' },
  ]}
/>

:::

</Exercise>

<Exercise title="Pass or fail" anchor="exercise-pass-or-fail">

:::tip Activity: Pass or fail
Write `grade(score)` so it returns `pass` if `score` is **at least 60**, otherwise `fail`.

<CodeExercise
  title="Pass or fail"
  heading="exercise-pass-or-fail"
  lang="python"
  filename="main.py"
  prompt="Return pass if score >= 60 else fail."
  sampleLog={`(input) 75
pass`}
  starter={`def grade(score):
    # TODO
    return "fail"
`}
  wrapSuffix={`
print(grade(int(input())))
`}
  tests={[
    { name: '75 pass', stdin: '75', equals: 'pass' },
    { name: '60 pass', stdin: '60', equals: 'pass' },
    { name: '59 fail', stdin: '59', equals: 'fail' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
