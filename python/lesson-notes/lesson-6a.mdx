---
sidebar_position: 11
title: Chapter 6a - For Loops
---

`for i in range(n):` repeats a block `n` times. `i` starts at `0`.

```python
for i in range(5):
    print(i)
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="240px"
  code={`for i in range(5):
    print(i)
`}
/>

</details>

`range(start, stop, step)` can count with a step: `range(2, 12, 3)` → 2, 5, 8, 11.

## Mini quiz — for

<MultipleChoice
  id="py-6a-for"
  title="for loops"
  questions={[
    {
      prompt: 'The first value of i is',
      code: 'for i in range(5):',
      codeLang: 'python',
      choices: ['1', '0', '5', '-1'],
      answer: 1,
      why: 'range starts at 0 unless you say otherwise.',
    },
    {
      prompt: 'Which number is in this range?',
      code: 'range(2, 12, 3)',
      codeLang: 'python',
      choices: ['12', '11', '10', '1'],
      answer: 1,
      why: '2, 5, 8, 11 — stop 12 is excluded.',
    },
  ]}
/>

## Challenge: Hello 1 to n

Write `hello(n)` so it **prints** `Hello 1` through `Hello n`, one per line. Start at 1, not 0.

Expected for `3`:
```
Hello 1
Hello 2
Hello 3
```

<CodingExam
  title="Hello 1 to n"
  heading="Challenge: Hello 1 to n"
  lang="python"
  filename="main.py"
  prompt="Print Hello 1 .. Hello n."
  sampleLog={`(input) 3
Hello 1
Hello 2
Hello 3`}
  starter={`def hello(n):
    # TODO: for i in range(1, n + 1):
    pass
`}
  wrapSuffix={`
hello(int(input()))
`}
  tests={[
    { name: 'n=3', stdin: '3', equals: 'Hello 1\nHello 2\nHello 3' },
    { name: 'n=1', stdin: '1', equals: 'Hello 1' },
  ]}
/>

## Mini quiz — range

<MultipleChoice
  id="py-6a-range"
  title="range"
  questions={[
    {
      prompt: 'How many times does this loop?',
      code: 'for i in range(5):',
      codeLang: 'python',
      choices: ['4', '5', '6', '0'],
      answer: 1,
      why: 'i is 0,1,2,3,4.',
    },
    {
      prompt: 'To count 1 through 5 you can use',
      code: `range(5)
range(1, 6)
range(1, 5)`,
      codeLang: 'python',
      choices: ['range(5)', 'range(1, 6)', 'range(1, 5)', 'range(0, 5)'],
      answer: 1,
      why: 'stop is exclusive, so 6 is the end.',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Sum 1 to n" anchor="exercise-sum-1-to-n">

:::tip Activity: Sum 1 to n
Write `total(n)` so it returns `1 + 2 + ... + n`.

Expected for `4`:
```
10
```

<CodingExam
  title="Sum 1 to n"
  heading="exercise-sum-1-to-n"
  lang="python"
  filename="main.py"
  prompt="Add 1 through n with a for loop."
  sampleLog={`(input) 4
10`}
  starter={`def total(n):
    s = 0
    # TODO
    return s
`}
  wrapSuffix={`
print(total(int(input())))
`}
  tests={[
    { name: '4 → 10', stdin: '4', equals: '10' },
    { name: '1 → 1', stdin: '1', equals: '1' },
    { name: '0 → 0', stdin: '0', equals: '0' },
  ]}
/>

:::

</Exercise>

<Exercise title="Set one table" anchor="exercise-set-one-table">

:::tip Activity: Set one table
Write `set_table()` so it **prints** exactly these four lines (one table, no loop required):

```
Plate placed
Silverware placed
Napkin placed
Glass of water placed
```

<CodingExam
  title="Set one table"
  heading="exercise-set-one-table"
  lang="python"
  filename="main.py"
  prompt="Print the four setup lines."
  sampleLog={`Plate placed
Silverware placed
Napkin placed
Glass of water placed`}
  starter={`def set_table():
    # TODO: four prints
    pass
`}
  wrapSuffix={`
set_table()
`}
  tests={[
    {
      name: 'four lines',
      stdin: '',
      equals: 'Plate placed\nSilverware placed\nNapkin placed\nGlass of water placed',
    },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
