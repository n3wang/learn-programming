---
sidebar_position: 13
title: Chapter 7a - While Loop
---

A `while` loop repeats **as long as** the test is true.

```python
n = 3
while n > 0:
    print(n)
    n = n - 1
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="240px"
  code={`n = 3
while n > 0:
    print(n)
    n = n - 1
print("done")
`}
/>

</details>

## Mini quiz — while test

<MultipleChoice
  id="py-7a-while-test"
  title="while test"
  questions={[
    {
      prompt: 'How many numbers are printed before done?',
      code: `n = 3
while n > 0:
    print(n)
    n = n - 1
print("done")`,
      codeLang: 'python',
      choices: ['2', '3', '4', '0'],
      answer: 1,
      why: '3, then 2, then 1.',
    },
    {
      prompt: 'What must change inside the loop so it can finish?',
      code: `n = 3
while n > 0:
    print(n)
    n = n - 1`,
      codeLang: 'python',
      choices: ['The print font', 'The test variable (here n)', 'Python itself', 'range()'],
      answer: 1,
      why: 'If n never changes, the test stays true forever.',
    },
  ]}
/>

## Challenge: countdown

Write `countdown(n)` so it prints `n`, then `n-1`, ... then `1` (do not print `0`).

Expected for `3`:
```
3
2
1
```

<CodingExam
  title="countdown"
  heading="Challenge: countdown"
  lang="python"
  filename="main.py"
  prompt="Print n down to 1."
  sampleLog={`(input) 3
3
2
1`}
  starter={`def countdown(n):
    # TODO: while n > 0
    pass
`}
  wrapSuffix={`
countdown(int(input()))
`}
  tests={[
    { name: '3', stdin: '3', equals: '3\n2\n1' },
    { name: '1', stdin: '1', equals: '1' },
  ]}
/>

## Mini quiz — while

<MultipleChoice
  id="py-7a-while"
  title="while"
  questions={[
    {
      prompt: 'When does a while loop stop?',
      choices: ['After 10 times', 'When the test becomes false', 'Never', 'At break only'],
      answer: 1,
      why: 'The test is checked every time around.',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Keep going?" anchor="exercise-keep-going">

:::tip Activity: Keep going?
Write `keep_going(letter)` so it returns `True` if the letter is `y`, otherwise `False`.
(This is the question you ask at the end of a `while` loop.)

<CodingExam
  title="Keep going?"
  heading="exercise-keep-going"
  lang="python"
  filename="main.py"
  prompt="Return True only for y."
  sampleLog={`(input) y
True`}
  starter={`def keep_going(letter):
    # TODO
    return False
`}
  wrapSuffix={`
print(keep_going(input().strip()))
`}
  tests={[
    { name: 'y', stdin: 'y', equals: 'True' },
    { name: 'n', stdin: 'n', equals: 'False' },
  ]}
/>

:::

</Exercise>

<Exercise title="Add until stop" anchor="exercise-add-until-stop">

:::tip Activity: Add until stop
Write `add_all(nums)` so it returns the sum of the numbers. Stop adding if you see a `0` (do not add the 0). Use a while loop or a for + break.

`3 4 0 9` → `7`

<CodingExam
  title="Add until stop"
  heading="exercise-add-until-stop"
  lang="python"
  filename="main.py"
  prompt="Sum until 0 (exclusive)."
  sampleLog={`(input) 3 4 0 9
7`}
  starter={`def add_all(nums):
    total = 0
    i = 0
    # TODO: while i < len(nums) and nums[i] != 0
    return total
`}
  wrapSuffix={`
nums = [int(x) for x in input().split()]
print(add_all(nums))
`}
  tests={[
    { name: '3 4 0 9', stdin: '3 4 0 9', equals: '7' },
    { name: '5 0', stdin: '5 0', equals: '5' },
    { name: '0 1', stdin: '0 1', equals: '0' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
