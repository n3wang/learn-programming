---
sidebar_position: 10
title: Chapter 5a - Random
---

`random.randrange(a, b)` picks an integer from `a` up to **but not including** `b`.

```python
import random
print(random.randrange(10, 20))  # 10 ... 19
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="python"
  height="220px"
  code={`import random
print(random.randrange(10, 20))
`}
/>

</details>

## Mini quiz — exclusive stop

<MultipleChoice
  id="py-5a-exclusive"
  title="Exclusive stop"
  questions={[
    {
      prompt: 'This call can never be which number?',
      code: 'random.randrange(10, 20)',
      codeLang: 'python',
      choices: ['10', '19', '20', '15'],
      answer: 2,
      why: 'The stop value is not included.',
    },
    {
      prompt: 'Which call can produce 30, 31, ... 39?',
      code: `random.randrange(30, 39)
random.randrange(30, 40)
random.randrange(29, 39)`,
      codeLang: 'python',
      choices: ['randrange(30, 39)', 'randrange(30, 40)', 'randrange(29, 39)', 'randrange(30)'],
      answer: 1,
      why: 'Stop must be 40 so 39 is allowed.',
    },
  ]}
/>

## Coin sides

`0` means heads, `1` means tails. Mapping that to words is just `if`.

## Challenge: coin word

Write `coin(n)` so it returns `Heads` if `n` is `0`, otherwise `Tails`.

<CodingExam
  title="coin word"
  heading="Challenge: coin word"
  lang="python"
  filename="main.py"
  prompt="0 → Heads, anything else → Tails."
  sampleLog={`(input) 0
Heads`}
  starter={`def coin(n):
    # TODO
    return ""
`}
  wrapSuffix={`
print(coin(int(input())))
`}
  tests={[
    { name: '0 Heads', stdin: '0', equals: 'Heads' },
    { name: '1 Tails', stdin: '1', equals: 'Tails' },
  ]}
/>

## Mini quiz — randrange

<MultipleChoice
  id="py-5a-randrange"
  title="randrange"
  questions={[
    {
      prompt: 'This call can be which of these?',
      code: 'random.randrange(1, 7)',
      codeLang: 'python',
      choices: ['7', '0', '6', '7.5'],
      answer: 2,
      why: 'Stop is exclusive, so 1 through 6.',
    },
    {
      prompt: 'A fair coin can use which call?',
      code: `random.randrange(0, 1)
random.randrange(0, 2)
random.randrange(1, 1)`,
      codeLang: 'python',
      choices: ['randrange(0, 1)', 'randrange(0, 2)', 'randrange(1, 1)', 'randrange(2, 2)'],
      answer: 1,
      why: '0 and 1 are the two faces.',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Team color" anchor="exercise-team-color">

:::tip Activity: Team color
Write `team(n)` so it returns `blue` if `n` is `0`, otherwise `red`.
(In a real program you would set `n = random.randrange(0, 2)`.)

<CodingExam
  title="Team color"
  heading="exercise-team-color"
  lang="python"
  filename="main.py"
  prompt="0 → blue, else red."
  sampleLog={`(input) 0
blue`}
  starter={`def team(n):
    # TODO
    return ""
`}
  wrapSuffix={`
print(team(int(input())))
`}
  tests={[
    { name: '0 blue', stdin: '0', equals: 'blue' },
    { name: '1 red', stdin: '1', equals: 'red' },
  ]}
/>

:::

</Exercise>

<Exercise title="D20 range" anchor="exercise-d20-range">

:::tip Activity: D20 range
A 20-sided die uses `randrange(1, 21)`. Write `valid_face(n)` so it returns `True` if `n` is between 1 and 20 inclusive.

<CodingExam
  title="D20 range"
  heading="exercise-d20-range"
  lang="python"
  filename="main.py"
  prompt="Return True if 1 <= n <= 20."
  sampleLog={`(input) 20
True`}
  starter={`def valid_face(n):
    # TODO
    return False
`}
  wrapSuffix={`
print(valid_face(int(input())))
`}
  tests={[
    { name: '20 ok', stdin: '20', equals: 'True' },
    { name: '1 ok', stdin: '1', equals: 'True' },
    { name: '0 no', stdin: '0', equals: 'False' },
    { name: '21 no', stdin: '21', equals: 'False' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
