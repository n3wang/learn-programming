---
sidebar_position: 6
title: Chapter 3a - Inputs
---

`input()` waits for the student to type a line. `int(input())` turns that line into a number.

<details>
<summary>🧪 Try it (type in the box under output, then Enter)</summary>

<PistonRunner
  lang="python"
  height="260px"
  code={`name = input("write something: ")
print(name)
`}
/>

</details>

:::important Among Us project
[Project 3a](../project/project-3a)
:::

## Numbers from the keyboard

```python
n = int(input())
print(n + 1)
```

## Mini quiz — converting input

<MultipleChoice
  id="py-3a-int-input"
  title="int and input"
  questions={[
    {
      prompt: 'If the student types 3, what is the result?',
      code: 'int(input()) + 1',
      codeLang: 'python',
      choices: ['31', '4', '"31"', 'Error'],
      answer: 1,
      why: '3 becomes an int, then plus one is 4.',
    },
    {
      prompt: 'What happens?',
      code: '"3" + 1',
      codeLang: 'python',
      choices: ['4', '31', 'TypeError', '3'],
      answer: 2,
      why: 'A string and an int cannot use + together.',
    },
  ]}
/>

## Challenge: add one

Write `plus_one(n)` so it **returns** `n + 1`.

<CodingExam
  title="add one"
  heading="Challenge: add one"
  lang="python"
  filename="main.py"
  prompt="Return n + 1."
  sampleLog={`(input) 7
8`}
  starter={`def plus_one(n):
    # TODO
    return 0
`}
  wrapSuffix={`
print(plus_one(int(input())))
`}
  tests={[
    { name: '7 → 8', stdin: '7', equals: '8' },
    { name: '0 → 1', stdin: '0', equals: '1' },
  ]}
/>

## Mini quiz — input

<MultipleChoice
  id="py-3a-input"
  title="input"
  questions={[
    {
      prompt: 'What type does this return?',
      code: 'input()',
      codeLang: 'python',
      choices: ['int', 'str', 'bool', 'float'],
      answer: 1,
      why: 'Keyboard text is always a string until you convert it.',
    },
    {
      prompt: 'How do you read a whole number?',
      code: 'int(input())',
      codeLang: 'python',
      choices: ['input(int)', 'int(input())', 'number()', 'cin'],
      answer: 1,
      why: 'Read the line, then convert with int().',
    },
  ]}
/>

## Exercises

<ExerciseSet>
<Exercise title="Hello name" anchor="exercise-hello-name">

:::tip Activity: Hello name
Write `hello(name)` so it **returns** `Hello, ` plus the name (comma and space).

Expected for `Ada`:
```
Hello, Ada
```

<CodingExam
  title="Hello name"
  heading="exercise-hello-name"
  lang="python"
  filename="main.py"
  prompt="Return Hello, plus the name."
  sampleLog={`(input) Ada
Hello, Ada`}
  starter={`def hello(name):
    # TODO
    return ""
`}
  wrapSuffix={`
print(hello(input()))
`}
  tests={[
    { name: 'Ada', stdin: 'Ada', equals: 'Hello, Ada' },
    { name: 'Lin', stdin: 'Lin', equals: 'Hello, Lin' },
  ]}
/>

:::

</Exercise>

<Exercise title="Bakery total" anchor="exercise-bakery-total">

:::tip Activity: Bakery total
Cakes cost **2** each. Write `bill(cakes)` so it returns the total cost.

Expected for `3`:
```
6
```

<CodingExam
  title="Bakery total"
  heading="exercise-bakery-total"
  lang="python"
  filename="main.py"
  prompt="Return 2 * cakes."
  sampleLog={`(input) 3
6`}
  starter={`def bill(cakes):
    # TODO
    return 0
`}
  wrapSuffix={`
print(bill(int(input())))
`}
  tests={[
    { name: '3 cakes → 6', stdin: '3', equals: '6' },
    { name: '0 → 0', stdin: '0', equals: '0' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
