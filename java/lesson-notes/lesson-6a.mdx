---
title: Chapter 6a - While Loops
---

:::note While Loop

![](../../static/img/2022-06-07-22-46-00.png)

The BooleanExpression is tested, and if it is true, the Statement is executed. Then, the BooleanExpression is tested again. If it is true, the Statement is executed. This cycle repeats until the BooleanExpression is false.

:::

## While Loop


```java
import java.util.*;
class Main {
  public static void main(String[] args) {
    int number = 0;
    while (number<5)
    {
        System.out.println("Hello");
        number++;
    }
    
  }
}
```




<details>
<summary>
🧪 Try the code out~!
</summary>
<iframe src="https://trinket.io/embed/java/0aa8aff94e" width="100%" height="600" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

</details>

![](../../static/img/2022-06-07-22-54-23.png)



### Practice

```java



import java.util.Random;
class Main {
   public static void main(String args[]) {
       
       int count = 10;
        while (count >= 1)
        {
            System.out.println("Hello World");
            System.out.println(count);
            count--;
        }
   }
}
```

How many times will that code print `Hello World`?

import RadioListAns from "@site/src/components/RadioListAns.js";

<RadioListAns checkList={["5 times", "10 times", "9 times"]} answer={2}></RadioListAns>


### Exercise: Throwing random numbers

:::tip Create a program that throws random numbers...

1. Create a program that generates random numbers from 0-9 (0 and 9 included) 5 times
2. Modify it so that it now generates 100 random numbers

***

You can create your program using the following template.

<iframe src="https://trinket.io/embed/java/eae3e912b1" width="100%" height="600" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

***


<details>
<summary>
Hint
</summary>

This is the code to print a random number from 0-100
<iframe src="https://trinket.io/embed/java/286df0d784" width="100%" height="600" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

</details>

🙋‍♀️ Expected Program (1) 5 random numbers from 0-9

<iframe src="https://trinket.io/embed/java/55795bac9c?outputOnly=true" width="100%" height="300" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>


🙋‍♀️ Expected Program (2) - 100 random numbers

<iframe src="https://trinket.io/embed/java/9befbc2c17?outputOnly=true" width="100%" height="600" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

:::



## Infinite Loops

:::caution Be careful and avoid creating an infinite loop

![](https://i.pinimg.com/originals/6c/11/e3/6c11e35213adf79ef32bc31f75897f58.gif)

- Infinite Loops

This code will be printing `Hello` forever



```java
import java.util.*;
class Main {
  public static void main(String[] args) {
   int number = 1;
    while (number <= 5)
    {
       System.out.println("Hello");
    }
    
  }
}
```

<details>
<summary>
🧪 Try the code out~!
</summary>
<iframe src="https://trinket.io/embed/java/df212d025f" width="100%" height="300" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

</details>

:::



## Using While Loops to Validate Inputs

![](../../static/img/2022-06-07-23-17-59.png)

```java

import java.util.*;
class Main {
  public static void main(String[] args) {
  int number;

  // Create a Scanner object for keyboard input.
  Scanner keyboard = new Scanner(System.in);
  
  // Get a number from the user.
  System.out.print("Enter a number in the range of 1 through 100: ");
  number = keyboard.nextInt();
  
  // Validate the input.
  while (number < 1 || number > 100)
  {
     System.out.print("Invalid input. Enter a number in the range " +
                      "of 1 through 100: ");
     number = keyboard.nextInt();
  }
    
  }
}

```
<details>
<summary>
🧪 Try the code out~!
</summary>
<iframe src="https://trinket.io/embed/java/ec17c20097" width="100%" height="400" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

</details>


### Exercise: Generate Random numbers until...

:::tip Exercise

- Create a program that generates random numbers from 0-9 until it generates number 5
- You can use the following template to start your code:

<iframe src="https://trinket.io/embed/java/baa22568ba" width="100%" height="600" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>

***

🙋‍♀️ Expected Program

<iframe src="https://trinket.io/embed/java/cf21d88f13?outputOnly=true" width="100%" height="300" frameborder="0" marginwidth="0" marginheight="0" allowfullscreen></iframe>


:::

---

### Try it: validating with a while loop

<PistonRunner
  lang="java"
  interactive={false}
  height="260px"
  code={`public class Main {
  public static void main(String[] args) {
    int attempts = 0;
    int number = -5; // pretend this came from user input
    while (number < 1 || number > 100) {
      attempts++;
      number = 42; // pretend the "next" input is valid
    }
    System.out.println("Valid after " + attempts + " retry/retries");
  }
}
`}
/>

<MultipleChoice
  id="java-ch6a-while"
  title="While loops"
  questions={[
    {
      prompt: 'int i = 0; while (i < 3) { System.out.println(i); i++; } How many times does it print?',
      code: 'int i = 0;\nwhile (i < 3) {\n  System.out.println(i);\n  i++;\n}',
      codeLang: 'java',
      choices: ['2', '3', '4', 'infinite'],
      answer: 1,
      why: 'i starts at 0 and the loop runs for i = 0, 1, 2 — three times — then stops once i becomes 3.',
    },
    {
      prompt: 'What is the most common cause of an accidental infinite while loop?',
      choices: [
        'Using >= instead of >',
        'Forgetting to update the variable the condition depends on',
        'Using a boolean condition',
        'Declaring the loop variable with int',
      ],
      answer: 1,
      why: 'If nothing inside the loop body changes the variable(s) in the condition, the condition never becomes false, and the loop never ends.',
    },
  ]}
/>

<CodingExam
  title="count down to zero"
  heading="Try it: count down to zero"
  lang="java"
  filename="Main.java"
  prompt="Read a positive integer n. Print every value from n down to 1, one per line, using a while loop."
  sampleLog={`(input) 3
3
2
1`}
  starter={`public class Main {
    public static void countdown(int n) {
        // TODO: while loop printing n, n-1, ..., 1
    }
}
`}
  wrapSuffix={`
class Runner {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        int n = sc.nextInt();
        Main.countdown(n);
    }
}
`}
  sourceChecks={[
    { name: 'Uses a while loop', pattern: 'while\\s*\\(', must: true, hint: 'while (n >= 1) { ... }' },
  ]}
  tests={[
    { name: 'n=3', stdin: '3', equals: '3\n2\n1' },
    { name: 'n=1', stdin: '1', equals: '1' },
  ]}
/>

---

## Chapter summary

:::important Key takeaways

1. A `while` loop repeats its body **as long as** its condition stays `true`, checking the condition before every pass.
2. Every `while` loop needs something inside its body that eventually makes the condition `false` — forgetting that update creates an infinite loop.
3. `while` is the natural fit when you do not know in advance how many times you will loop (validating input, waiting for a specific random value).
4. Contrast with a `for` loop (next chapter), which is the natural fit when you already know exactly how many times to repeat.

:::

## Exercises

<ExerciseSet>
<Exercise title="Sum until sentinel" anchor="exercise-sum-sentinel">

:::tip Activity: Sum until sentinel
Read integers one at a time until you read `0` (the "sentinel" value, not included in the sum). Print the total sum of everything read before the `0`.

<CodingExam
  title="Sum until sentinel"
  heading="exercise-sum-sentinel"
  lang="java"
  filename="Main.java"
  prompt="while the next int is not 0, add it to a running total."
  sampleLog={`(input) 1 2 3 0
6`}
  starter={`import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // TODO: read ints with a while loop until you read 0, print the sum
    }
}
`}
  sourceChecks={[
    { name: 'Uses a while loop', pattern: 'while\\s*\\(', must: true, hint: 'while (n != 0) { ... }' },
  ]}
  tests={[
    { name: '1 2 3 0', stdin: '1 2 3 0', equals: '6' },
    { name: 'immediate zero', stdin: '0', equals: '0' },
  ]}
/>

:::

</Exercise>

<Exercise title="Validate a range" anchor="exercise-validate-range">

:::tip Activity: Validate a range
You are given a list of attempted values (space-separated integers, last one always valid: `1-100`). Print `"invalid"` once for every value that is out of range, then print the final valid value.

<CodingExam
  title="Validate a range"
  heading="exercise-validate-range"
  lang="java"
  filename="Main.java"
  prompt="Read integers with a while loop; while a value is out of 1-100, print invalid; stop and print the first in-range value."
  sampleLog={`(input) 200 -5 42
invalid
invalid
42`}
  starter={`import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // TODO
    }
}
`}
  sourceChecks={[
    { name: 'Uses a while loop', pattern: 'while\\s*\\(', must: true, hint: 'while (value < 1 || value > 100) { ... }' },
  ]}
  tests={[
    { name: 'two invalid then valid', stdin: '200 -5 42', equals: 'invalid\ninvalid\n42' },
    { name: 'first try valid', stdin: '50', equals: '50' },
  ]}
/>

:::

</Exercise>

<Exercise title="Powers of two" anchor="exercise-powers-of-two">

:::tip Activity: Powers of two
Read an integer `limit`. Print every power of two (starting from `1`) that is less than or equal to `limit`, one per line, using a `while` loop.

<CodingExam
  title="Powers of two"
  heading="exercise-powers-of-two"
  lang="java"
  filename="Main.java"
  prompt="Start value at 1, while value <= limit print it and multiply by 2."
  sampleLog={`(input) 20
1
2
4
8
16`}
  starter={`public class Main {
    public static void powersOfTwo(int limit) {
        // TODO
    }
}
`}
  wrapSuffix={`
class Runner {
    public static void main(String[] args) {
        java.util.Scanner sc = new java.util.Scanner(System.in);
        int limit = sc.nextInt();
        Main.powersOfTwo(limit);
    }
}
`}
  sourceChecks={[
    { name: 'Uses a while loop', pattern: 'while\\s*\\(', must: true, hint: 'while (value <= limit) { ... }' },
  ]}
  tests={[
    { name: 'limit 20', stdin: '20', equals: '1\n2\n4\n8\n16' },
    { name: 'limit 1', stdin: '1', equals: '1' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>










