/**
 * Dedicated sidebar config for the 'fundamentals' docs plugin instance only
 * (see docusaurus.config.js). Kept separate from sidebars.js because that
 * file is shared by every other docs plugin instance (python, java,
 * robotics, scratch, ...) — an explicit category with no matching docs
 * throws a hard build error for those instances, so this content-specific
 * structure has to live on its own.
 */

// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // The original, established fundamentals categories share one combined
  // sidebar — each wrapped in its own explicit top-level category, so their
  // subcategories stay grouped underneath the right heading instead of all
  // flattening into one mixed list.
  tutorialSidebar: [
    {
      type: 'category',
      label: 'Artificial Intelligence',
      items: [{type: 'autogenerated', dirName: 'artificial-intelligence'}],
    },
    {
      type: 'category',
      label: 'Computer Engineering',
      items: [{type: 'autogenerated', dirName: 'computer-engineering'}],
    },
    {
      type: 'category',
      label: 'Algorithms',
      items: [{type: 'autogenerated', dirName: 'algorithms'}],
    },
    {
      type: 'category',
      label: 'Game Engine',
      items: [{type: 'autogenerated', dirName: 'game-engine-development'}],
    },
    {
      type: 'category',
      label: 'Graphics',
      items: [{type: 'autogenerated', dirName: 'graphics'}],
    },
    {
      type: 'category',
      label: 'Scalable Systems',
      items: [{type: 'autogenerated', dirName: 'scalable-systems'}],
    },
  ],

  // Newer sections each get their own isolated sidebar, so visiting one
  // only shows its own content while it's still being built out — not the
  // entire fundamentals tree above.
  mathAndScienceSidebar: [{type: 'autogenerated', dirName: 'math-and-science'}],
  electronicsSidebar: [{type: 'autogenerated', dirName: 'electronics'}],
  financeSidebar: [{type: 'autogenerated', dirName: 'finance'}],
};

module.exports = sidebars;
