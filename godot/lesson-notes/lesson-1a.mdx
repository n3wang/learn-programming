---
sidebar_position: 2
title: Chapter 1a - First script
---

## Your first GDScript

Every sandbox script starts the same way: extend `SceneTree`, put code in `_init()`, then `quit()`.

```python
extends SceneTree

func _init():
    print("Hello Godot")
    quit()
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="godot"
  filename="main.gd"
  height="260px"
  code={[
    'extends SceneTree',
    '',
    'func _init():',
    '    print("Hello Godot")',
    '    quit()',
    '',
  ].join('\n')}
/>

</details>

## Mini quiz — entry point

<MultipleChoice
  id="gd-1a-entry"
  title="Entry point"
  questions={[
    {
      prompt: 'Why must sandbox scripts call quit()?',
      choices: [
        'Godot prints OK only after quit',
        'Otherwise the SceneTree keeps running until timeout',
        'print does not work without quit',
        'extends SceneTree requires it at compile time',
      ],
      answer: 1,
      why: 'Headless Godot keeps the main loop alive until you quit.',
    },
    {
      prompt: 'Where does this sandbox start running your logic?',
      choices: ['_ready()', '_process()', '_init()', 'main()'],
      answer: 2,
      why: 'For SceneTree scripts, _init() runs as soon as the tree is created.',
    },
  ]}
/>

## Printing

`print` writes to the output. Strings use double quotes. You can print several values in one call.

```python
print("score", 12)
print("a", "b", "c")
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="godot"
  filename="main.gd"
  height="280px"
  code={[
    'extends SceneTree',
    '',
    'func _init():',
    '    print("score", 12)',
    '    print("Hi", "Ada")',
    '    quit()',
    '',
  ].join('\n')}
/>

</details>

## Challenge: print a product

Complete `_init` so it **prints** the result of `6 * 7` (one number), then quits.

:::tip Indentation
Indent with **4 spaces** (Tab in the editor inserts spaces). Click **Reset** if an old draft has broken indent.
:::

<CodeExercise
  title="print a product"
  heading="Challenge: print a product"
  lang="godot"
  filename="main.gd"
  prompt="Print 6 * 7. Hidden tests check the output is 42."
  sampleLog={`42`}
  starter={[
    'extends SceneTree',
    '',
    'func _init():',
    '    print(0)',
    '    quit()',
    '',
  ].join('\n')}
  sourceChecks={[
    {
      name: 'Uses print',
      pattern: '\\bprint\\s*\\(',
      must: true,
      hint: 'print(...)',
    },
  ]}
  tests={[
    { name: 'prints 42', equals: '42' },
  ]}
/>

## Comments

`#` starts a comment to the end of the line.

```python
# This is a comment
print("still runs")  # trailing comment
```

## Exercises

Three steps: print two lines, print a labeled score, then build a line from two variables.

<ExerciseSet>
<Exercise title="Two lines" anchor="exercise-two-lines">

:::tip Activity: Two lines
Print `Hi` on the first line and `Ada` on the second line.

```
Hi
Ada
```

<CodeExercise
  title="Two lines"
  heading="exercise-two-lines"
  lang="godot"
  filename="main.gd"
  prompt="Print Hi then Ada on two lines."
  sampleLog={`Hi
Ada`}
  starter={[
    'extends SceneTree',
    '',
    'func _init():',
    '    pass',
    '    quit()',
    '',
  ].join('\n')}
  sourceChecks={[
    {
      name: 'Uses print',
      pattern: '\\bprint\\s*\\(',
      must: true,
      hint: 'Call print at least once',
    },
  ]}
  tests={[
    { name: 'Hi then Ada', equals: 'Hi\nAda' },
  ]}
/>

:::

</Exercise>

<Exercise title="Score label" anchor="exercise-score-label">

:::tip Activity: Score label
Print exactly `score 42` using one `print` call with two arguments (a string and a number), like `print("score", 42)`.

```
score 42
```

<CodeExercise
  title="Score label"
  heading="exercise-score-label"
  lang="godot"
  filename="main.gd"
  prompt="Print score and 42 as two print arguments."
  sampleLog={`score 42`}
  starter={[
    'extends SceneTree',
    '',
    'func _init():',
    '    print("")',
    '    quit()',
    '',
  ].join('\n')}
  sourceChecks={[
    {
      name: 'print with two args',
      pattern: 'print\\s*\\([^,\\n]+,[^,\\n]+\\)',
      must: true,
      hint: 'print("score", 42)',
    },
  ]}
  tests={[
    { name: 'score 42', equals: 'score 42' },
  ]}
/>

:::

</Exercise>

<Exercise title="Battle cry" anchor="exercise-battle-cry">

:::tip Activity: Battle cry
Print three lines:

```
Ready
Fight
Win
```

Use three separate `print` calls (one per line).

<CodeExercise
  title="Battle cry"
  heading="exercise-battle-cry"
  lang="godot"
  filename="main.gd"
  prompt="Print Ready, Fight, Win on three lines."
  sampleLog={`Ready
Fight
Win`}
  starter={[
    'extends SceneTree',
    '',
    'func _init():',
    '    # TODO: three prints',
    '    quit()',
    '',
  ].join('\n')}
  sourceChecks={[
    {
      name: 'At least three prints',
      pattern: '(?s)print\\s*\\(.*print\\s*\\(.*print\\s*\\(',
      must: true,
      hint: 'Call print three times',
    },
  ]}
  tests={[
    { name: 'three lines', equals: 'Ready\nFight\nWin' },
  ]}
/>

:::

</Exercise>
</ExerciseSet>
