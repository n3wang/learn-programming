---
sidebar_position: 126
title: 12.10b Shor labs
sidebar_label: '12.10b Shor labs'
---

# 12.10b Shor labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §12.10. Pure Python (`math.gcd`). Continues [12.10](./12-10-shor).

## Explorer

<FormulaExplorerTabs preset="shorPhaseToPeriod" />


## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="qc-shor" />

<ExerciseSet>
<Exercise title="12.10b.1 gcd(8,15)" anchor="exercise-cp-1210-gcd">

:::tip Activity: Coprime check
Print $\gcd(8,15)$.

No input.

<CodeExercise
  title="gcd"
  heading="exercise-cp-1210-gcd"
  lang="python"
  filename="main.py"
  prompt="1"
  sampleLog={`1`}
  starter={`import math

g = 0  # TODO
`}
  wrapSuffix={`
print(g)
`}
  hint="math.gcd(8, 15)."
  solution={`import math

g = math.gcd(8, 15)
`}
  sourceChecks={[
    {name: 'gcd', pattern: 'gcd', must: true, hint: 'use math.gcd'},
  ]}
  tests={[
    {name: 'g', equals: '1'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.10b.2 Phase 0.25 → T" anchor="exercise-cp-1210-t">

:::tip Activity: Denominator
If QPE returns $\phi=0.25=1/4$, the period candidate $T$ is? Print $T$.

No input.

<CodeExercise
  title="T from 1/4"
  heading="exercise-cp-1210-t"
  lang="python"
  filename="main.py"
  prompt="4"
  sampleLog={`4`}
  starter={`T = 0  # TODO
`}
  wrapSuffix={`
print(T)
`}
  hint="φ = S/T with S=1 → T=4."
  solution={`T = 4
`}
  sourceChecks={[
    {name: 'four', pattern: '4', must: true, hint: 'T=4'},
  ]}
  tests={[
    {name: 'T', equals: '4'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.10b.3 Factor via gcd" anchor="exercise-cp-1210-fac">

:::tip Activity: $r^{T/2}-1$
For $r=8$, $T=4$, $N=15$, print $\gcd(r^{T//2}-1,\,N)$.

No input.

<CodeExercise
  title="factor"
  heading="exercise-cp-1210-fac"
  lang="python"
  filename="main.py"
  prompt="3"
  sampleLog={`3`}
  starter={`import math

r, T, N = 8, 4, 15
f = 0  # TODO
`}
  wrapSuffix={`
print(f)
`}
  hint="gcd(8**2 - 1, 15) = gcd(63,15)=3."
  solution={`import math

r, T, N = 8, 4, 15
f = math.gcd(r ** (T // 2) - 1, N)
`}
  sourceChecks={[
    {name: 'gcd', pattern: 'gcd', must: true, hint: 'math.gcd'},
  ]}
  tests={[
    {name: 'f', equals: '3'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.10b.4 Other factor" anchor="exercise-cp-1210-fac2">

:::tip Activity: Companion factor
Print $\gcd(r^{T//2}+1,\,N)$ for the same $r,T,N$.

No input.

<CodeExercise
  title="factor+"
  heading="exercise-cp-1210-fac2"
  lang="python"
  filename="main.py"
  prompt="5"
  sampleLog={`5`}
  starter={`import math

r, T, N = 8, 4, 15
f = 0  # TODO
`}
  wrapSuffix={`
print(f)
`}
  hint="gcd(65, 15)=5."
  solution={`import math

r, T, N = 8, 4, 15
f = math.gcd(r ** (T // 2) + 1, N)
`}
  sourceChecks={[
    {name: 'gcd', pattern: 'gcd', must: true, hint: 'math.gcd'},
  ]}
  tests={[
    {name: 'f', equals: '5'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.10b.5 8² mod 15" anchor="exercise-cp-1210-mod">

:::tip Activity: Modular square
Print $8^2\bmod 15$.

No input.

<CodeExercise
  title="mod"
  heading="exercise-cp-1210-mod"
  lang="python"
  filename="main.py"
  prompt="4"
  sampleLog={`4`}
  starter={`v = 0  # TODO
`}
  wrapSuffix={`
print(v)
`}
  hint="64 % 15."
  solution={`v = (8 ** 2) % 15
`}
  sourceChecks={[
    {name: 'mod', pattern: '%|mod', must: true, hint: 'modulo'},
  ]}
  tests={[
    {name: 'v', equals: '4'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.10b.6 Product check" anchor="exercise-cp-1210-prod">

:::tip Activity: 3×5
Print $3\times 5$ to confirm $N=15$ factors.

No input.

<CodeExercise
  title="3*5"
  heading="exercise-cp-1210-prod"
  lang="python"
  filename="main.py"
  prompt="15"
  sampleLog={`15`}
  starter={`p = 0  # TODO
`}
  wrapSuffix={`
print(p)
`}
  hint="3*5."
  solution={`p = 3 * 5
`}
  sourceChecks={[
    {name: 'mul', pattern: '\\*|15', must: true, hint: 'product'},
  ]}
  tests={[
    {name: 'p', equals: '15'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-1210b-assess">
  <Problem
    type="concept"
    title="12.10b.W1"
    minWords={3}
    keywords={['period', 'modular', 'factor']}
    prompt={`How does finding the period of rˣ mod N help factor N?`}
    answer={`From an even period T one forms gcd(r^{T/2}±1, N), which are nontrivial factors when the usual conditions hold.`}
  />
  <Problem
    type="concept"
    title="12.10b.W2"
    minWords={3}
    keywords={['qft', 'phase', 'estimation']}
    prompt={`Where does the inverse QFT appear in Shor’s pipeline?`}
    answer={`Inside quantum phase estimation: after controlled-U powers, IQFT on the control register reads the phase bits.`}
  />
  <Problem
    type="concept"
    title="12.10b.W3"
    minWords={3}
    keywords={['odd', 'retry', 'period']}
    prompt={`If the recovered period T is odd, what does Shor’s outline say to do?`}
    answer={`Discard that r and retry with a new random base (odd T does not yield the ±1 gcd trick).`}
  />
</ProblemSet>

Next: [12.11 QC code sketches](./12-11-qc-code-sketches).
