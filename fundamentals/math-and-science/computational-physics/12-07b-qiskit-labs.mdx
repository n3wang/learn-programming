---
sidebar_position: 120
title: 12.7b Qiskit labs
sidebar_label: '12.7b Qiskit labs'
---

# 12.7b Qiskit / Bell labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §12.7. Pure Python — no Qiskit in the sandbox. Continues [12.7](./12-07-qiskit).

## Explorer

<FormulaExplorerTabs preset="qcHadamard" />

## Amplitudes and ordering


## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="qc-qiskit-bell" />

<ExerciseSet>
<Exercise title="12.7b.1 Bell amp" anchor="exercise-cp-127-bell">

:::tip Activity: $1/\sqrt{2}$
Print the $|00\rangle$ amplitude of $|\beta_{00}\rangle$ with **10 decimals**.

No input.

<CodeExercise
  title="β₀₀ amp"
  heading="exercise-cp-127-bell"
  lang="python"
  filename="main.py"
  prompt="0.7071067812"
  sampleLog={`0.7071067812`}
  starter={`import math

a00 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{a00:.10f}")
`}
  hint="Equal weight on |00⟩ and |11⟩."
  solution={`import math

a00 = 1.0 / math.sqrt(2.0)
`}
  sourceChecks={[
    {name: 'sqrt', pattern: 'sqrt|\\*\\*', must: true, hint: '1/√2'},
  ]}
  tests={[
    {name: 'a', equals: '0.7071067812'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.7b.2 Separability of Bell" anchor="exercise-cp-127-sep">

:::tip Activity: $|wz-xy|$
For $w=z=1/\sqrt2$, $x=y=0$, print $|wz-xy|$ with **10 decimals**.

No input.

<CodeExercise
  title="|wz−xy|"
  heading="exercise-cp-127-sep"
  lang="python"
  filename="main.py"
  prompt="0.5"
  sampleLog={`0.5000000000`}
  starter={`import math

w = z = 1.0 / math.sqrt(2.0)
x = y = 0.0
meas = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{meas:.10f}")
`}
  hint="abs(w*z - x*y)."
  solution={`import math

w = z = 1.0 / math.sqrt(2.0)
x = y = 0.0
meas = abs(w * z - x * y)
`}
  sourceChecks={[
    {name: 'abs', pattern: 'abs|fabs', must: true, hint: 'absolute value'},
  ]}
  tests={[
    {name: 'm', equals: '0.5000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.7b.3 IBM bit order" anchor="exercise-cp-127-endian">

:::tip Activity: Rightmost = q0
In IBM ordering $|q_1 q_0\rangle$, the bitstring `01` means which value of $q_0$? Print `0` or `1`.

No input.

<CodeExercise
  title="q0 from 01"
  heading="exercise-cp-127-endian"
  lang="python"
  filename="main.py"
  prompt="1"
  sampleLog={`1`}
  starter={`q0 = -1  # TODO
`}
  wrapSuffix={`
print(q0)
`}
  hint="Rightmost bit is q0."
  solution={`q0 = 1
`}
  sourceChecks={[
    {name: 'one', pattern: '1', must: true, hint: 'q0=1'},
  ]}
  tests={[
    {name: 'q0', equals: '1'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.7b.4 Ideal P(00) for Bell" anchor="exercise-cp-127-p00">

:::tip Activity: Shot probability
Ideal $|\beta_{00}\rangle$: print $P(00)$ with **10 decimals**.

No input.

<CodeExercise
  title="P(00)"
  heading="exercise-cp-127-p00"
  lang="python"
  filename="main.py"
  prompt="0.5"
  sampleLog={`0.5000000000`}
  starter={`p = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{p:.10f}")
`}
  hint="|1/√2|²."
  solution={`p = 0.5
`}
  sourceChecks={[
    {name: 'half', pattern: '0\\.5|1/2', must: true, hint: '1/2'},
  ]}
  tests={[
    {name: 'p', equals: '0.5000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.7b.5 Gate count H+CX" anchor="exercise-cp-127-gates">

:::tip Activity: Depth sketch
How many one- and two-qubit gate applications in the minimal Bell circuit (H then CX)? Print the integer count.

No input.

<CodeExercise
  title="gate count"
  heading="exercise-cp-127-gates"
  lang="python"
  filename="main.py"
  prompt="2"
  sampleLog={`2`}
  starter={`n = 0  # TODO
`}
  wrapSuffix={`
print(n)
`}
  hint="One H and one CNOT."
  solution={`n = 2
`}
  sourceChecks={[
    {name: 'two', pattern: '2', must: true, hint: '2 gates'},
  ]}
  tests={[
    {name: 'n', equals: '2'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Wire Bell (Composer / Qiskit order)

Same schema-style wiring as [12.4](./12-04-logic-gates): start → H → CNOT → out.

<NodeLab
  id="cp-127b-bell-wire"
  mode="wiring"
  height={280}
  nodes={[
    {
      id: 'start',
      position: {x: 20, y: 80},
      data: {title: 'start', variant: 'table', rows: [{text: '|00⟩', badge: 'in'}]},
    },
    {
      id: 'h',
      position: {x: 200, y: 80},
      data: {title: 'H', variant: 'table', rows: [{text: 'q[0]', badge: 'U'}]},
    },
    {
      id: 'cx',
      position: {x: 380, y: 80},
      data: {title: 'CX', variant: 'table', rows: [{text: '0→1', badge: 'U'}]},
    },
    {
      id: 'bell',
      position: {x: 560, y: 80},
      data: {title: 'out', variant: 'table', rows: [{text: '|β₀₀⟩', badge: 'out'}]},
    },
  ]}
  edges={[]}
  solution={{
    requiredEdges: [
      {
        source: 'start',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'h',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: '|00⟩ → H',
      },
      {
        source: 'h',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'cx',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: 'H → CX',
      },
      {
        source: 'cx',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'bell',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: 'CX → Bell',
      },
    ],
  }}
/>

## Write-ups

<ProblemSet id="cp-127b-assess">
  <Problem
    type="concept"
    title="12.7b.W1"
    minWords={3}
    keywords={['simulator', 'noise', 'hardware']}
    prompt={`Why might a hardware histogram for |β₀₀⟩ disagree with a statevector simulator?`}
    answer={`Real devices have decoherence and gate/readout errors that leak probability into other bitstrings; the ideal simulator does not.`}
  />
  <Problem
    type="concept"
    title="12.7b.W2"
    minWords={3}
    keywords={['transpile', 'coupling', 'native']}
    prompt={`What does transpiling a circuit onto an IBM backend accomplish?`}
    answer={`It rewrites the circuit into the device’s native gates and respects the qubit coupling map so the job can run on that hardware.`}
  />
  <Problem
    type="concept"
    title="12.7b.W3"
    minWords={3}
    keywords={['token', 'secret', 'api']}
    prompt={`Why must an IBM API token stay out of public git repositories?`}
    answer={`Anyone with the token can use your quota / account; treat it like a password and load it from a local secret store.`}
  />
</ProblemSet>

Next: [12.8 Quantum Fourier transform](./12-08-qft).
