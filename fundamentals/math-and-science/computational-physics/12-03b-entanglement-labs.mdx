---
sidebar_position: 113
title: 12.3b Entanglement labs
sidebar_label: '12.3b Entanglement labs'
---

# 12.3b Entanglement labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §12.3. Original drills — **not** an `Entangle.py` dump. Continues [12.3](./12-03-entanglement).

## Explorer

<FormulaExplorerTabs preset="qcSeparability" />

Pure Python.

## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="qc-entanglement-bell" />


## Labs

<ExerciseSet>
<Exercise title="12.3b.1 Bell amplitude" anchor="exercise-cp-123-bell">

:::tip Activity: $1/\sqrt{2}$
Print $1/\sqrt{2}$ with **10 decimals**.

No input.

<CodeExercise
  title="1/sqrt(2)"
  heading="exercise-cp-123-bell"
  lang="python"
  filename="main.py"
  prompt="1/sqrt(2); 10 decimals."
  sampleLog={`0.7071067812`}
  starter={`import math

s = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{s:.10f}")
`}
  hint="1 / math.sqrt(2)."
  solution={`import math

s = 1.0 / math.sqrt(2.0)
`}
  sourceChecks={[
    {name: 'sqrt', pattern: 'sqrt|\\*\\*', must: true, hint: 'square root'},
  ]}
  tests={[
    {name: 's', equals: '0.7071067812'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.2 Separability measure" anchor="exercise-cp-123-sep">

:::tip Activity: $|wz-xy|$ for $\beta_{00}$
For $w=z=1/\sqrt{2}$, $x=y=0$, print $|wz-xy|$ with **10 decimals** (nonzero ⇒ entangled).

No input.

<CodeExercise
  title="|wz-xy|"
  heading="exercise-cp-123-sep"
  lang="python"
  filename="main.py"
  prompt="abs(wz-xy) for Bell 00; 10 decimals."
  sampleLog={`0.5000000000`}
  starter={`import math

s = 1.0 / math.sqrt(2.0)
w, x, y, z = s, 0.0, 0.0, s
meas = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{meas:.10f}")
`}
  hint="abs(w*z - x*y)."
  solution={`import math

s = 1.0 / math.sqrt(2.0)
w, x, y, z = s, 0.0, 0.0, s
meas = abs(w * z - x * y)
`}
  sourceChecks={[
    {name: 'abs', pattern: 'abs', must: true, hint: 'absolute difference'},
  ]}
  tests={[
    {name: 'meas', equals: '0.5000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.3 Product state passes test" anchor="exercise-cp-123-prod">

:::tip Activity: $|0\rangle\otimes|0\rangle$
$w=1,x=y=z=0$. Print $|wz-xy|$ with **10 decimals**.

No input.

<CodeExercise
  title="Separable measure"
  heading="exercise-cp-123-prod"
  lang="python"
  filename="main.py"
  prompt="|wz-xy| for |00⟩."
  sampleLog={`0.0000000000`}
  starter={`w, x, y, z = 1.0, 0.0, 0.0, 0.0
meas = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{meas:.10f}")
`}
  hint="Should be 0."
  solution={`w, x, y, z = 1.0, 0.0, 0.0, 0.0
meas = abs(w * z - x * y)
`}
  sourceChecks={[
    {name: 'abs', pattern: 'abs|\\*', must: true, hint: 'wz - xy'},
  ]}
  tests={[
    {name: 'meas', equals: '0.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.4 $X\otimes X$ corner" anchor="exercise-cp-123-xx">

:::tip Activity: Entry $(0,3)$
Print $(X\otimes X)_{0,3}$ (0-based) with **10 decimals**.

No input.

<CodeExercise
  title="XX[0,3]"
  heading="exercise-cp-123-xx"
  lang="python"
  filename="main.py"
  prompt="1.0 — anti-diagonal corner."
  sampleLog={`1.0000000000`}
  starter={`# X⊗X has 1 at corners (0,3) and (3,0)
entry = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{entry:.10f}")
`}
  hint="That entry is 1."
  solution={`entry = 1.0
`}
  sourceChecks={[
    {name: 'one', pattern: '1', must: true, hint: 'matrix entry'},
  ]}
  tests={[
    {name: 'e', equals: '1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.5 Dipole $H_{00}$" anchor="exercise-cp-123-h00">

:::tip Activity: Scaled Hamiltonian
Print $H_{00}/(\mu^2/r^3)=-2$ with **10 decimals**.

No input.

<CodeExercise
  title="H00"
  heading="exercise-cp-123-h00"
  lang="python"
  filename="main.py"
  prompt="-2 with 10 decimals."
  sampleLog={`-2.0000000000`}
  starter={`H00 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{H00:.10f}")
`}
  hint="Top-left entry."
  solution={`H00 = -2.0
`}
  sourceChecks={[
    {name: 'val', pattern: '-?2', must: true, hint: '-2'},
  ]}
  tests={[
    {name: 'H00', equals: '-2.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.6 Dipole $H_{12}$" anchor="exercise-cp-123-h12">

:::tip Activity: Off-diagonal
Print $H_{1,2}/(\mu^2/r^3)$ with **10 decimals**.

No input.

<CodeExercise
  title="H12"
  heading="exercise-cp-123-h12"
  lang="python"
  filename="main.py"
  prompt="2.0"
  sampleLog={`2.0000000000`}
  starter={`H12 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{H12:.10f}")
`}
  hint="The coupled |01⟩–|10⟩ block."
  solution={`H12 = 2.0
`}
  sourceChecks={[
    {name: 'val', pattern: '2', must: true, hint: '2'},
  ]}
  tests={[
    {name: 'H12', equals: '2.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.7 Largest eigenvalue" anchor="exercise-cp-123-eig">

:::tip Activity: Spectrum
Print the largest eigenvalue of $H/(\mu^2/r^3)$ as an integer (`4`).

No input.

<CodeExercise
  title="λ_max"
  heading="exercise-cp-123-eig"
  lang="python"
  filename="main.py"
  prompt="4"
  sampleLog={`4`}
  starter={`lam_max = 0  # TODO
`}
  wrapSuffix={`
print(lam_max)
`}
  hint="Eigenvalues 4, 0, −2, −2."
  solution={`lam_max = 4
`}
  sourceChecks={[
    {name: 'four', pattern: '4', must: true, hint: 'largest is 4'},
  ]}
  tests={[
    {name: 'lam', equals: '4'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.3b.8 Pauli $X$ on $|0\rangle$" anchor="exercise-cp-123-x">

:::tip Activity: Bit flip
$X|0\rangle=|1\rangle=[0,1]$. Print both components with **10 decimals**.

No input.

<CodeExercise
  title="X|0⟩"
  heading="exercise-cp-123-x"
  lang="python"
  filename="main.py"
  prompt="0 1"
  sampleLog={`0.0000000000 1.0000000000`}
  starter={`out0, out1 = 0.0, 0.0  # TODO
`}
  wrapSuffix={`
print(f"{out0:.10f} {out1:.10f}")
`}
  hint="Result is |1⟩."
  solution={`out0, out1 = 0.0, 1.0
`}
  sourceChecks={[
    {name: 'vals', pattern: '0|1', must: true, hint: '|1⟩ components'},
  ]}
  tests={[
    {name: 'x0', equals: '0.0000000000 1.0000000000'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-123b-assess">
  <Problem
    type="concept"
    title="12.3b.W1"
    minWords={3}
    keywords={['bell', 'entangle', 'separable']}
    prompt={`Why is |β₀₀⟩ entangled by the wz=xy test?`}
    answer={`Amplitudes give w=z=1/√2 and x=y=0 so wz=1/2 ≠ xy=0; it is not a product state.`}
  />
  <Problem
    type="concept"
    title="12.3b.W2"
    minWords={3}
    keywords={['eigen', 'bell', 'dipole']}
    prompt={`Of the dipole eigenstates |00⟩, |11⟩, and (|01⟩±|10⟩)/√2, which are entangled?`}
    answer={`The Bell-like (|01⟩±|10⟩)/√2 are entangled; |00⟩ and |11⟩ are separable product states.`}
  />
</ProblemSet>

Next: [12.4 Logic gates](./12-04-logic-gates).
