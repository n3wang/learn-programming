---
sidebar_position: 82
title: 10.5b DWT labs
sidebar_label: '10.5b DWT labs'
---

# 10.5b DWT labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §10.5. Original drills. Continues [10.5](./10-05-discrete-wavelet-transforms).

## Explorer

<FormulaExplorerTabs preset="daub4Coeffs" />

Pure Python. No NumPy / VPython.

## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="dwt-pyramid-daub4" />


## Labs

<ExerciseSet>
<Exercise title="10.5b.1 Dyadic scale" anchor="exercise-cp-105-s">

:::tip Activity: $s=2^j$
For $j=3$, print $s=2^j$ as an integer.

No input.

<CodeExercise
  title="s = 2**j"
  heading="exercise-cp-105-s"
  lang="python"
  filename="main.py"
  prompt="j=3; print 2**j."
  sampleLog={`8`}
  starter={`j = 3
s = 0  # TODO
`}
  wrapSuffix={`
print(s)
`}
  hint="2**3 = 8."
  solution={`j = 3
s = 2 ** j
`}
  sourceChecks={[
    {name: 'power', pattern: '\\*\\*|pow', must: true, hint: 'power of two'},
  ]}
  tests={[
    {name: 's', equals: '8'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.2 Dyadic τ" anchor="exercise-cp-105-tau">

:::tip Activity: $\tau=k/2^{j}$
For $k=1$, $j=2$, print $\tau=k/2^{j}$ with **10 decimals**.

No input.

<CodeExercise
  title="tau = k / 2**j"
  heading="exercise-cp-105-tau"
  lang="python"
  filename="main.py"
  prompt="k=1, j=2; print k/2**j with 10 decimals."
  sampleLog={`0.2500000000`}
  starter={`k, j = 1, 2
tau = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{tau:.10f}")
`}
  hint="1/4 = 0.25."
  solution={`k, j = 1, 2
tau = k / (2 ** j)
`}
  sourceChecks={[
    {name: 'divides', pattern: '/', must: true, hint: 'k / 2**j'},
  ]}
  tests={[
    {name: 'tau', equals: '0.2500000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.3 Daub4 $c_0$" anchor="exercise-cp-105-c0">

:::tip Activity: First tap
Print $c_0=(1+\sqrt3)/(4\sqrt2)$ with **10 decimals**.

No input.

<CodeExercise
  title="Daub4 c0"
  heading="exercise-cp-105-c0"
  lang="python"
  filename="main.py"
  prompt="Print Daub4 c0 with 10 decimals."
  sampleLog={`0.4829629131`}
  starter={`import math

c0 = 0.0  # TODO: (1+sqrt(3))/(4*sqrt(2))
`}
  wrapSuffix={`
print(f"{c0:.10f}")
`}
  hint="Use math.sqrt(3) and math.sqrt(2)."
  solution={`import math

c0 = (1.0 + math.sqrt(3.0)) / (4.0 * math.sqrt(2.0))
`}
  sourceChecks={[
    {name: 'sqrt', pattern: 'sqrt', must: true, hint: '√3 and √2'},
  ]}
  tests={[
    {name: 'c0', equals: '0.4829629131'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.4 Orthogonality check" anchor="exercise-cp-105-norm">

:::tip Activity: $\sum c_i^2$
With all four Daub4 taps, print $c_0^2+c_1^2+c_2^2+c_3^2$ with **10 decimals**.

No input.

<CodeExercise
  title="Sum of squares"
  heading="exercise-cp-105-norm"
  lang="python"
  filename="main.py"
  prompt="Daub4: print sum of c_i**2 with 10 decimals."
  sampleLog={`1.0000000000`}
  starter={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
tot = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{tot:.10f}")
`}
  hint="Should be 1 for an orthogonal filter."
  solution={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
tot = c0 * c0 + c1 * c1 + c2 * c2 + c3 * c3
`}
  sourceChecks={[
    {name: 'squares', pattern: '\\*|\\*\\*', must: true, hint: 'sum squares'},
  ]}
  tests={[
    {name: 'norm1', equals: '1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.5 Highpass kills ramp" anchor="exercise-cp-105-H">

:::tip Activity: $H$ on $[0,1,2,3]$
With Daub4 $H=(c_3,-c_2,c_1,-c_0)$, print $H\cdot[0,1,2,3]$ with **10 decimals**.

No input.

<CodeExercise
  title="H on linear ramp"
  heading="exercise-cp-105-H"
  lang="python"
  filename="main.py"
  prompt="Daub4 H·[0,1,2,3]; print with 10 decimals (~0)."
  sampleLog={`0.0000000000`}
  starter={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
y = [0.0, 1.0, 2.0, 3.0]
d = 1.0  # TODO: c3*y0 - c2*y1 + c1*y2 - c0*y3
`}
  wrapSuffix={`
print(f"{d:.10f}")
`}
  hint="Vanishing moments ⇒ ~0."
  solution={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
y = [0.0, 1.0, 2.0, 3.0]
d = c3 * y[0] - c2 * y[1] + c1 * y[2] - c0 * y[3]
`}
  sourceChecks={[
    {name: 'H pattern', pattern: 'c3|c2|c1|c0', must: true, hint: 'use all four taps with H signs'},
  ]}
  tests={[
    {name: 'Hramp', equals: '0.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.6 Lowpass on ones" anchor="exercise-cp-105-L">

:::tip Activity: $L$ on $[1,1,1,1]$
Print $L\cdot[1,1,1,1]=c_0+c_1+c_2+c_3$ with **10 decimals**.

No input.

<CodeExercise
  title="L on constants"
  heading="exercise-cp-105-L"
  lang="python"
  filename="main.py"
  prompt="Sum of Daub4 c_i; 10 decimals."
  sampleLog={`1.4142135624`}
  starter={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
s = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{s:.10f}")
`}
  hint="Equals √2."
  solution={`import math

s3, s2 = math.sqrt(3.0), math.sqrt(2.0)
c0 = (1.0 + s3) / (4.0 * s2)
c1 = (3.0 + s3) / (4.0 * s2)
c2 = (3.0 - s3) / (4.0 * s2)
c3 = (1.0 - s3) / (4.0 * s2)
s = c0 + c1 + c2 + c3
`}
  sourceChecks={[
    {name: 'sums', pattern: '\\+', must: true, hint: 'add the four taps'},
  ]}
  tests={[
    {name: 'Lones', equals: '1.4142135624'},
  ]}
/>

:::

</Exercise>

<Exercise title="10.5b.7 Pyramid stage count" anchor="exercise-cp-105-stages">

:::tip Activity: Stages $N=8\to 2$
Starting from length 8, each ↓2 on the smooth branch halves length. Print how many filter stages until 2 smooth samples remain ($\log_2 N-1$).

No input.

<CodeExercise
  title="Pyramid stages"
  heading="exercise-cp-105-stages"
  lang="python"
  filename="main.py"
  prompt="N=8; print log2(N)-1 as int."
  sampleLog={`2`}
  starter={`import math

N = 8
stages = 0  # TODO
`}
  wrapSuffix={`
print(stages)
`}
  hint="3−1=2."
  solution={`import math

N = 8
stages = int(math.log2(N)) - 1
`}
  sourceChecks={[
    {name: 'log2', pattern: 'log2', must: true, hint: 'log base 2'},
  ]}
  tests={[
    {name: 'stages', equals: '2'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-105b-assess">
  <Problem
    type="concept"
    title="10.5b.W1"
    minWords={3}
    keywords={['detail', 'smooth', 'pyramid']}
    prompt={`In one sentence, what do the H and L branches of the DWT pyramid store?`}
    answer={`H (highpass) stores detail coefficients at that scale; L (lowpass) stores a smoothed, downsampled signal that is filtered again at the next coarser scale.`}
  />
  <Problem
    type="concept"
    title="10.5b.W2"
    minWords={3}
    keywords={['inverse', 'upsample', 'transpose']}
    prompt={`How do you invert a Daub4 pyramid transform?`}
    answer={`Upsample and apply the transpose filter matrix at each level, merging smooth and detail until the original length is restored; check by round-tripping a chirp or impulse.`}
  />
</ProblemSet>

Next: [10.6 Principal components analysis](./10-06-principal-components).
