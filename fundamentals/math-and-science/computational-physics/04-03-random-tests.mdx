---
sidebar_position: 11
title: 4.3 Testing random distributions
sidebar_label: '4.3 Testing random distributions'
---

# 4.3 Testing random distributions

> **Source:** Ideas from *Computational Physics* (Landau, Páez, Bordeianu), §4.4. Rewritten in original wording. Continues [4.2b Decay labs](./04-02b-decay-labs). Labs: [4.3b Random-distribution test labs](./04-03b-random-test-labs).

A computer’s “random” stream follows a definite rule, so successive values are **correlated**. That can poison a Monte Carlo that assumes independent draws. Habit: run a few uniformity / randomness checks — ideally **alongside** the simulation that depends on them.

## Look first

The cheapest test is to **look**. A uniform generator on $[0,1)$ should spit values that clearly sit in that interval, differ from each other, and show no cartoon pattern like $0.3333$ forever.

Sample from the chapter LCG (`r ← (1103515245 r + 12345) % 2³¹`, then $r/2^{31}$), seed $1$:

| | | | |
| --- | --- | --- | --- |
| 0.513870 | 0.175741 | 0.308652 | 0.534534 |
| 0.947628 | 0.171736 | 0.702231 | 0.226431 |
| 0.494773 | 0.124720 | 0.083899 | 0.389647 |

Your eye already sees $[0,1)$ coverage and no short repeating motif. That is not a proof — only a smoke test.

## Visual structure tests

1. **$r_i$ versus $i$** (Fig. 4.2 style). Expect a cloud filling the strip $0\le r<1$ with jitter, not a smooth curve.
2. **Successive-pair scatter** $(r_i,r_{i+1})$ or $(r_{2i},r_{2i+1})$ (Fig. 4.1 style). Random uniforms fill the unit square as a featureless cloud. A lattice or stripes ⇒ reject for serious work.

<FormulaExplorer preset="lcgVsIndex" />

<FormulaExplorer preset="lcgScatter" />

## Moment test for uniformity

The $k$th sample moment of $N$ draws is

$$
\langle x^{k}\rangle=\frac{1}{N}\sum_{i=1}^{N} x_i^{k}.
$$

For a uniform density on $[0,1]$,

$$
\langle x^{k}\rangle \simeq \int_0^1 x^{k}\,dx=\frac{1}{k+1}+O\!\left(\frac{1}{\sqrt{N}}\right).
$$

If the sample moment sits near $1/(k+1)$, the stream looks **uniform**. If the deviation shrinks like $1/\sqrt{N}$ (equivalently $\sqrt{N}\,|\mathrm{error}|$ stays $O(1)$), the mismatches look like ordinary sampling noise — consistent with **randomness** as well as uniformity.

Practical checklist: $k=1,3,7$ and $N=10^2,10^4,10^5$; print

$$
\sqrt{N}\left|\frac{1}{N}\sum_i x_i^{k}-\frac{1}{k+1}\right|
$$

and confirm it is order one.

<FormulaExplorer preset="rngMoment" />

## Lag-product correlation $C(k)$

Near-neighbor dependence shows up in

$$
C(k)=\frac{1}{N}\sum_{i=1}^{N} x_i\, x_{i+k}
\qquad (k=1,2,\ldots).
$$

Independent uniforms have joint density $1$ on the unit square, so

$$
C(k)\simeq\int_0^1\!\!\int_0^1 xy\,dx\,dy=\frac{1}{4}.
$$

Matching $1/4$ with $\sqrt{N}|C-1/4|=O(1)$ supports independence at lag $k$. Still plot the scatter: a bad LCG can pass crude averages while failing the eye test.

<FormulaExplorer preset="rngCorrCk" />

<GuidedChoiceExplanation preset="rng-uniform-tests" />

<MultipleChoice
  id="cp-43-rng-tests"
  title="RNG tests"
  questions={[
    {
      prompt: 'For uniform[0,1] draws, ⟨x³⟩ should approach…',
      choices: ['1/2', '1/3', '1/4', '1'],
      answer: 2,
      why: '∫₀¹ x³ dx = 1/4.',
    },
    {
      prompt: '√N |⟨xᵏ⟩ − 1/(k+1)| staying O(1) as N grows suggests…',
      choices: [
        'the generator is biased by a fixed offset',
        'deviations look like ordinary random sampling error',
        'the sequence is constant',
        'k must be even',
      ],
      answer: 1,
      why: '1/√N fluctuation scaling is the random-sampling signature.',
    },
    {
      prompt: 'Independent uniforms give C(k) ≈ …',
      choices: ['0', '1/2', '1/4', '1'],
      answer: 2,
      why: '∬ xy = 1/4.',
    },
    {
      type: 'order',
      prompt: 'Order a preflight RNG checklist (top = first).',
      items: [
        'Compute moments / C(k) and check √N|error| is O(1)',
        'Glance at raw values in [0,1)',
        'Scatter successive pairs; reject lattices',
        'Plot r_i vs i for coverage and jitter',
      ],
      order: [1, 3, 2, 0],
      why: 'Look, plot vs i, scatter, then quantitative moments/lags.',
    },
  ]}
/>

Next: [4.3b Random-distribution test labs](./04-03b-random-test-labs) — moment and $C(k)$ drills on the chapter LCG.
