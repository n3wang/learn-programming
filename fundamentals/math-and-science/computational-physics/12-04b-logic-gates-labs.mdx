---
sidebar_position: 115
title: 12.4b Logic gates labs
sidebar_label: '12.4b Gate labs'
---

# 12.4b Logic gates labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §12.4. Classical `NodeLab` truth-table checks + pure-Python unitary locks. Continues [12.4](./12-04-logic-gates).

## Explorer

<FormulaExplorerTabs preset="qcHadamard" />

## Classical wiring (NodeLab)

### Half-adder sum again (AND + OR + NAND path optional)

Build $Y=A\oplus B$ with **XOR** from the deck (or the AND/OR/NAND recipe from digital logic). Toggle inputs live; **Check** exhausts the truth table.

<NodeLab
  id="cp-124b-xor"
  mode="logic"
  height={380}
  palette={true}
  nodes={[
    {id: 'a', position: {x: 40, y: 50}, data: {variant: 'input', title: 'A', value: true}},
    {id: 'b', position: {x: 40, y: 240}, data: {variant: 'input', title: 'B', value: false}},
    {id: 'y', position: {x: 580, y: 140}, data: {variant: 'output', title: 'Y'}},
  ]}
  edges={[]}
  truthTable={{
    inputIds: ['a', 'b'],
    outputId: 'y',
    expected: (a, b) => a !== b,
  }}
/>

### NOR as “neither”

$Y=\overline{A+B}$ — drag **NOR**, wire both inputs.

<NodeLab
  id="cp-124b-nor"
  mode="logic"
  height={360}
  palette={true}
  nodes={[
    {id: 'a', position: {x: 40, y: 50}, data: {variant: 'input', title: 'A', value: false}},
    {id: 'b', position: {x: 40, y: 240}, data: {variant: 'input', title: 'B', value: false}},
    {id: 'y', position: {x: 580, y: 140}, data: {variant: 'output', title: 'Y'}},
  ]}
  edges={[]}
  truthTable={{
    inputIds: ['a', 'b'],
    outputId: 'y',
    expected: (a, b) => !(a || b),
  }}
/>

### Wire Bell again (schema-style)

Field-to-field: `|00⟩ → H → CNOT → |β₀₀⟩`. Same wiring checker as [schema design](/fundamentals/data-science/lesson-11-schema-design).

<NodeLab
  id="cp-124b-bell-wire"
  mode="wiring"
  height={300}
  nodes={[
    {
      id: 'start',
      position: {x: 20, y: 80},
      data: {
        title: 'start',
        variant: 'table',
        rows: [{text: '|00⟩', badge: 'in'}],
      },
    },
    {
      id: 'h',
      position: {x: 200, y: 80},
      data: {
        title: 'H',
        variant: 'table',
        rows: [{text: 'on q₀', badge: 'U'}],
      },
    },
    {
      id: 'cx',
      position: {x: 380, y: 80},
      data: {
        title: 'CNOT',
        variant: 'table',
        rows: [{text: 'ctrl=q₀ tgt=q₁', badge: 'U'}],
      },
    },
    {
      id: 'bell',
      position: {x: 560, y: 80},
      data: {
        title: 'out',
        variant: 'table',
        rows: [{text: '|β₀₀⟩', badge: 'out'}],
      },
    },
  ]}
  edges={[]}
  solution={{
    requiredEdges: [
      {
        source: 'start',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'h',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: '|00⟩ → H',
      },
      {
        source: 'h',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'cx',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: 'H → CNOT',
      },
      {
        source: 'cx',
        sourceHandle: ['row-0-left', 'row-0-right'],
        target: 'bell',
        targetHandle: ['row-0-left', 'row-0-right'],
        label: 'CNOT → |β₀₀⟩',
      },
    ],
  }}
/>

## Quantum unitaries (CodeExercise)

Pure Python. No Cirq / Qiskit in the sandbox.


## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="qc-logic-gates" />

<ExerciseSet>
<Exercise title="12.4b.1 Hadamard H₀₀" anchor="exercise-cp-124-h00">

:::tip Activity: $H_{00}=1/\sqrt{2}$
Print the top-left entry of the Hadamard matrix with **10 decimals**.

No input.

<CodeExercise
  title="H[0,0]"
  heading="exercise-cp-124-h00"
  lang="python"
  filename="main.py"
  prompt="1/sqrt(2); 10 decimals."
  sampleLog={`0.7071067812`}
  starter={`import math

h00 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{h00:.10f}")
`}
  hint="1 / math.sqrt(2)."
  solution={`import math

h00 = 1.0 / math.sqrt(2.0)
`}
  sourceChecks={[
    {name: 'sqrt', pattern: 'sqrt|\\*\\*', must: true, hint: '1/√2'},
  ]}
  tests={[
    {name: 'h00', equals: '0.7071067812'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.2 Hadamard H₁₁" anchor="exercise-cp-124-h11">

:::tip Activity: $H_{11}=-1/\sqrt{2}$
Print with **10 decimals**.

No input.

<CodeExercise
  title="H[1,1]"
  heading="exercise-cp-124-h11"
  lang="python"
  filename="main.py"
  prompt="-1/sqrt(2); 10 decimals."
  sampleLog={`-0.7071067812`}
  starter={`import math

h11 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{h11:.10f}")
`}
  hint="Negative of 1/√2."
  solution={`import math

h11 = -1.0 / math.sqrt(2.0)
`}
  sourceChecks={[
    {name: 'neg', pattern: '-', must: true, hint: 'negative entry'},
  ]}
  tests={[
    {name: 'h11', equals: '-0.7071067812'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.3 X|0⟩" anchor="exercise-cp-124-x">

:::tip Activity: NOT gate
Print components of $X|0\rangle=|1\rangle$ with **10 decimals**.

No input.

<CodeExercise
  title="X|0⟩"
  heading="exercise-cp-124-x"
  lang="python"
  filename="main.py"
  prompt="0 1"
  sampleLog={`0.0000000000 1.0000000000`}
  starter={`out0, out1 = 0.0, 0.0  # TODO
`}
  wrapSuffix={`
print(f"{out0:.10f} {out1:.10f}")
`}
  hint="Bit flip of |0⟩."
  solution={`out0, out1 = 0.0, 1.0
`}
  sourceChecks={[
    {name: 'vals', pattern: '0|1', must: true, hint: '|1⟩'},
  ]}
  tests={[
    {name: 'x0', equals: '0.0000000000 1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.4 Phase $R_\pi$ on |1⟩" anchor="exercise-cp-124-phase">

:::tip Activity: $e^{i\pi}$
Print the real part of $e^{i\pi}$ with **10 decimals** (phase factor on $|1\rangle$).

No input.

<CodeExercise
  title="e^(iπ)"
  heading="exercise-cp-124-phase"
  lang="python"
  filename="main.py"
  prompt="real part of exp(iπ)."
  sampleLog={`-1.0000000000`}
  starter={`import cmath
import math

z = cmath.exp(1j * math.pi)
re = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{re:.10f}")
`}
  hint="z.real after exp."
  solution={`import cmath
import math

z = cmath.exp(1j * math.pi)
re = z.real
`}
  sourceChecks={[
    {name: 'exp', pattern: 'exp|pi', must: true, hint: 'complex exp'},
  ]}
  tests={[
    {name: 're', equals: '-1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.5 S-gate factor" anchor="exercise-cp-124-s">

:::tip Activity: $e^{i\pi/2}=i$
Print real and imaginary parts of $e^{i\pi/2}$ space-separated with **10 decimals**.

No input.

<CodeExercise
  title="S phase"
  heading="exercise-cp-124-s"
  lang="python"
  filename="main.py"
  prompt="0 1 for i."
  sampleLog={`0.0000000000 1.0000000000`}
  starter={`import cmath
import math

z = cmath.exp(1j * math.pi / 2)
`}
  wrapSuffix={`
print(f"{z.real:.10f} {z.imag:.10f}")
`}
  hint="Should be approximately 0 + 1j."
  solution={`import cmath
import math

z = cmath.exp(1j * math.pi / 2)
`}
  sourceChecks={[
    {name: 'exp', pattern: 'exp', must: true, hint: 'exp(i π/2)'},
  ]}
  tests={[
    {name: 's', equals: '0.0000000000 1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.6 CNOT|10⟩" anchor="exercise-cp-124-cnot">

:::tip Activity: Target flip
Basis order $|00\rangle,|01\rangle,|10\rangle,|11\rangle$. After CNOT (control=left), $|10\rangle$ becomes which basis index (0–3)?

No input.

<CodeExercise
  title="CNOT index"
  heading="exercise-cp-124-cnot"
  lang="python"
  filename="main.py"
  prompt="|11⟩ is index 3."
  sampleLog={`3`}
  starter={`# |10> = index 2 → CNOT → |11> = index 3
idx = -1  # TODO
`}
  wrapSuffix={`
print(idx)
`}
  hint="|11⟩ is the last basis vector."
  solution={`idx = 3
`}
  sourceChecks={[
    {name: 'three', pattern: '3', must: true, hint: 'index of |11⟩'},
  ]}
  tests={[
    {name: 'idx', equals: '3'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.7 CZ on |11⟩" anchor="exercise-cp-124-cz">

:::tip Activity: Sign flip
$CZ|11\rangle=-|11\rangle$. Print the phase factor with **10 decimals**.

No input.

<CodeExercise
  title="CZ phase"
  heading="exercise-cp-124-cz"
  lang="python"
  filename="main.py"
  prompt="-1"
  sampleLog={`-1.0000000000`}
  starter={`phase = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{phase:.10f}")
`}
  hint="Minus one."
  solution={`phase = -1.0
`}
  sourceChecks={[
    {name: 'neg', pattern: '-', must: true, hint: '-1'},
  ]}
  tests={[
    {name: 'ph', equals: '-1.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="12.4b.8 Toffoli |110⟩→|111⟩" anchor="exercise-cp-124-toff">

:::tip Activity: Basis index
In 8-D order $|000\rangle\ldots|111\rangle$, $|110\rangle$ is index 6 and maps to $|111\rangle$ at index…?

No input.

<CodeExercise
  title="Toffoli index"
  heading="exercise-cp-124-toff"
  lang="python"
  filename="main.py"
  prompt="7"
  sampleLog={`7`}
  starter={`idx = -1  # TODO
`}
  wrapSuffix={`
print(idx)
`}
  hint="|111⟩ is the last of eight."
  solution={`idx = 7
`}
  sourceChecks={[
    {name: 'seven', pattern: '7', must: true, hint: 'index 7'},
  ]}
  tests={[
    {name: 'idx', equals: '7'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-124b-assess">
  <Problem
    type="concept"
    title="12.4b.W1"
    minWords={3}
    keywords={['half', 'adder', 'xor', 'and']}
    prompt={`Which classical gates implement a half-adder’s sum and carry?`}
    answer={`Sum = XOR of the two bits; carry = AND of the two bits.`}
  />
  <Problem
    type="concept"
    title="12.4b.W2"
    minWords={3}
    keywords={['hadamard', 'cnot', 'bell']}
    prompt={`Outline the two-gate circuit that turns |00⟩ into |β₀₀⟩.`}
    answer={`Apply H to the first qubit, then CNOT with that qubit as control and the second as target.`}
  />
  <Problem
    type="concept"
    title="12.4b.W3"
    minWords={3}
    keywords={['unitary', 'measure', 'probability']}
    prompt={`Why are quantum gates unitary, and why is measurement not a gate in that sense?`}
    answer={`Unitaries preserve the state norm (probabilities); measurement collapses the state and is not a reversible unitary map.`}
  />
</ProblemSet>

Next: [12.5 Cirq / QC programming](./12-05-qc-programming).
