---
sidebar_position: 98
title: 11.6b TF / sklearn labs
sidebar_label: '11.6b TF / sklearn labs'
---

# 11.6b TensorFlow / sklearn labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §11.5. Pure-Python locks mirroring TF / sklearn arithmetic — **not** listing dumps. Continues [11.6](./11-06-tensorflow-sklearn-examples).

## Explorer

<FormulaExplorerTabs preset="nnMassExcess" />

Pure Python. No TensorFlow / sklearn / NumPy required.

## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="nn-tf-physics-examples" />


## Labs

<ExerciseSet>
<Exercise title="11.6b.1 Mass excess $^1$H" anchor="exercise-cp-116-excess">

:::tip Activity: $(M-A)\times 931.494028$
With $M=1.007827032$, $A=1$, print the mass excess in MeV/$c^2$ with **10 decimals**.

No input.

<CodeExercise
  title="Mass excess H-1"
  heading="exercise-cp-116-excess"
  lang="python"
  filename="main.py"
  prompt="(M-A)*931.494028; 10 decimals."
  sampleLog={`7.2908335650`}
  starter={`M, A = 1.007827032, 1.0
U_TO_MEV = 931.494028
excess = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{excess:.10f}")
`}
  hint="Subtract A, multiply by the conversion."
  solution={`M, A = 1.007827032, 1.0
U_TO_MEV = 931.494028
excess = (M - A) * U_TO_MEV
`}
  sourceChecks={[
    {name: 'scale', pattern: '\\*', must: true, hint: 'convert u difference to MeV'},
  ]}
  tests={[
    {name: 'ex', equals: '7.2908335650'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.6b.2 Mass excess $^2$H" anchor="exercise-cp-116-d">

:::tip Activity: Deuterium
$M=2.01401778$, $A=2$. Print excess with **10 decimals**.

No input.

<CodeExercise
  title="Mass excess H-2"
  heading="exercise-cp-116-d"
  lang="python"
  filename="main.py"
  prompt="(M-2)*931.494028; 10 decimals."
  sampleLog={`13.0574783558`}
  starter={`M, A = 2.01401778, 2.0
U_TO_MEV = 931.494028
excess = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{excess:.10f}")
`}
  hint="Same formula as H-1."
  solution={`M, A = 2.01401778, 2.0
U_TO_MEV = 931.494028
excess = (M - A) * U_TO_MEV
`}
  sourceChecks={[
    {name: 'scale', pattern: '\\*', must: true, hint: '(M-A)*factor'},
  ]}
  tests={[
    {name: 'ex', equals: '13.0574783558'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.6b.3 GradientTape mirror" anchor="exercise-cp-116-grad">

:::tip Activity: $\partial\mathcal{L}/\partial x$
$z=mx+b$, $\mathcal{L}=(y-z)^2$, with $m=1.5$, $b=2.2$, $x=0.5$, $y=1.8$.
Print $\partial\mathcal{L}/\partial x=2(y-z)(-m)$ with **10 decimals**.

No input.

<CodeExercise
  title="dL/dx"
  heading="exercise-cp-116-grad"
  lang="python"
  filename="main.py"
  prompt="2*(y-z)*(-m); 10 decimals."
  sampleLog={`3.4500000000`}
  starter={`m, b, x, y = 1.5, 2.2, 0.5, 1.8
z = m * x + b
dL_dx = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{dL_dx:.10f}")
`}
  hint="2 * (y - z) * (-m)."
  solution={`m, b, x, y = 1.5, 2.2, 0.5, 1.8
z = m * x + b
dL_dx = 2.0 * (y - z) * (-m)
`}
  sourceChecks={[
    {name: 'chain', pattern: '\\*', must: true, hint: 'chain rule through residual'},
  ]}
  tests={[
    {name: 'dLdx', equals: '3.4500000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.6b.4 Hubble MSE one point" anchor="exercise-cp-116-mse">

:::tip Activity: $(v- (H r + b))^2$
With $v=1000$, $r=2$, $H=400$, $b=50$, print the squared residual with **10 decimals**.

No input.

<CodeExercise
  title="One-point MSE term"
  heading="exercise-cp-116-mse"
  lang="python"
  filename="main.py"
  prompt="(v - (H*r+b))**2; 10 decimals."
  sampleLog={`22500.0000000000`}
  starter={`v, r, H, b = 1000.0, 2.0, 400.0, 50.0
err2 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{err2:.10f}")
`}
  hint="Predict H*r+b; square the difference."
  solution={`v, r, H, b = 1000.0, 2.0, 400.0, 50.0
err2 = (v - (H * r + b)) ** 2
`}
  sourceChecks={[
    {name: 'sq', pattern: '\\*\\*|pow', must: true, hint: 'square the residual'},
  ]}
  tests={[
    {name: 'err2', equals: '22500.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.6b.5 Poly feature $A^2$" anchor="exercise-cp-116-poly">

:::tip Activity: Degree-2 feature
For $A=3$, print the row $(A, A^2)$ as two numbers with **10 decimals**.

No input.

<CodeExercise
  title="Poly features"
  heading="exercise-cp-116-poly"
  lang="python"
  filename="main.py"
  prompt="A and A**2 for A=3; 10 decimals."
  sampleLog={`3.0000000000 9.0000000000`}
  starter={`A = 3.0
f1 = 0.0  # TODO
f2 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{f1:.10f} {f2:.10f}")
`}
  hint="A and A*A."
  solution={`A = 3.0
f1 = A
f2 = A * A
`}
  sourceChecks={[
    {name: 'sq', pattern: '\\*|\\*\\*', must: true, hint: 'A squared'},
  ]}
  tests={[
    {name: 'feat', equals: '3.0000000000 9.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.6b.6 Tabulated $B/A$ mean" anchor="exercise-cp-116-ba">

:::tip Activity: Mean of seven $B/A$ values
Using
`[0.0140, 1.1520, 2.8235, 1.8150, 1.3871, 0.9465, 1.2971]`,
print the mean with **10 decimals**.

No input.

<CodeExercise
  title="Mean B/A"
  heading="exercise-cp-116-ba"
  lang="python"
  filename="main.py"
  prompt="mean of B list; 10 decimals."
  sampleLog={`1.3478857143`}
  starter={`B = [0.0140, 1.1520, 2.8235, 1.8150, 1.3871, 0.9465, 1.2971]
mean_B = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{mean_B:.10f}")
`}
  hint="sum(B)/len(B)."
  solution={`B = [0.0140, 1.1520, 2.8235, 1.8150, 1.3871, 0.9465, 1.2971]
mean_B = sum(B) / len(B)
`}
  sourceChecks={[
    {name: 'avg', pattern: 'sum|/', must: true, hint: 'average the list'},
  ]}
  tests={[
    {name: 'mean', equals: '1.3478857143'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-116b-assess">
  <Problem
    type="concept"
    title="11.6b.W1"
    minWords={3}
    keywords={['mass', 'excess', 'binding']}
    prompt={`How does mass excess differ from binding energy per nucleon?`}
    answer={`Mass excess is M−A (in energy units); binding energy measures how much less the nucleus weighs than its separated nucleons — related but not the same plot.`}
  />
  <Problem
    type="concept"
    title="11.6b.W2"
    minWords={3}
    keywords={['gradient', 'tape', 'autodiff']}
    prompt={`What problem does GradientTape solve compared to hand-written ∂ℒ/∂w?`}
    answer={`It records the forward computation and returns analytic gradients automatically, avoiding manual chain-rule algebra for every parameter.`}
  />
  <Problem
    type="concept"
    title="11.6b.W3"
    minWords={3}
    keywords={['poly', 'overfit', 'degree']}
    prompt={`Why might a high-degree polynomial fit to seven B/A points look poor scientifically?`}
    answer={`Few noisy points + high degree overfits; nuclear binding is not a simple polynomial in A.`}
  />
</ProblemSet>

Next: [11.7 ML clustering](./11-07-ml-clustering).
