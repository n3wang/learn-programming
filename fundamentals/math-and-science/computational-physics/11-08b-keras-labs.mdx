---
sidebar_position: 102
title: 11.8b Keras labs
sidebar_label: '11.8b Keras labs'
---

# 11.8b Keras labs

> **Source:** Lab ideas from *Computational Physics* (Landau, Páez, Bordeianu), §11.7. Pure-Python mirrors of Dense / linear fit pieces. Continues [11.8](./11-08-keras-deep-learning).

## Explorer

<FormulaExplorerTabs preset="nnDenseLinear" />

Pure Python. No TensorFlow / Keras in the sandbox.

## Formula refresh

Warm up: pick the matching equation among lookalikes (same walk as the theory lesson).

<GuidedChoiceExplanation preset="nn-keras-dense" />


## Labs

<ExerciseSet>
<Exercise title="11.8b.1 Dense linear map" anchor="exercise-cp-118-dense">

:::tip Activity: $y=wx+b$
With $w=448.52048$, $b=-34.726036$, $x=1$, print $y$ with **10 decimals**.

No input.

<CodeExercise
  title="Dense units=1"
  heading="exercise-cp-118-dense"
  lang="python"
  filename="main.py"
  prompt="w*x+b; 10 decimals."
  sampleLog={`413.7944440000`}
  starter={`w, b, x = 448.52048, -34.726036, 1.0
y = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{y:.10f}")
`}
  hint="One weight, one bias."
  solution={`w, b, x = 448.52048, -34.726036, 1.0
y = w * x + b
`}
  sourceChecks={[
    {name: 'affine', pattern: '\\*|\\+', must: true, hint: 'w*x + b'},
  ]}
  tests={[
    {name: 'y', equals: '413.7944440000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.8b.2 ReLU in a layer" anchor="exercise-cp-118-relu">

:::tip Activity: Layer activation
Print $\mathrm{ReLU}(-1)$ and $\mathrm{ReLU}(2)$ space-separated with **10 decimals**.

No input.

<CodeExercise
  title="ReLU"
  heading="exercise-cp-118-relu"
  lang="python"
  filename="main.py"
  prompt="max(0,z) for -1 and 2."
  sampleLog={`0.0000000000 2.0000000000`}
  starter={`a = 0.0  # TODO
b = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{a:.10f} {b:.10f}")
`}
  hint="max(0.0, z)."
  solution={`a = max(0.0, -1.0)
b = max(0.0, 2.0)
`}
  sourceChecks={[
    {name: 'max', pattern: 'max', must: true, hint: 'ReLU'},
  ]}
  tests={[
    {name: 'relu', equals: '0.0000000000 2.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.8b.3 MSE for one prediction" anchor="exercise-cp-118-mse">

:::tip Activity: Compile loss piece
Print $(v_{\mathrm{true}}-v_{\mathrm{pred}})^2$ for $500$ vs $480$ with **10 decimals**.

No input.

<CodeExercise
  title="Squared error"
  heading="exercise-cp-118-mse"
  lang="python"
  filename="main.py"
  prompt="(500-480)**2; 10 decimals."
  sampleLog={`400.0000000000`}
  starter={`err2 = 0.0  # TODO
`}
  wrapSuffix={`
print(f"{err2:.10f}")
`}
  hint="Square the residual."
  solution={`err2 = (500.0 - 480.0) ** 2
`}
  sourceChecks={[
    {name: 'sq', pattern: '\\*\\*|pow', must: true, hint: 'square'},
  ]}
  tests={[
    {name: 'err2', equals: '400.0000000000'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.8b.4 Parameter count (tiny MLP)" anchor="exercise-cp-118-params">

:::tip Activity: Weights + biases
A Dense map $\mathbb{R}^2\to\mathbb{R}^3$ has $2\times 3$ weights and $3$ biases. Print the total parameter count.

No input.

<CodeExercise
  title="Param count"
  heading="exercise-cp-118-params"
  lang="python"
  filename="main.py"
  prompt="2*3 + 3."
  sampleLog={`9`}
  starter={`nin, nout = 2, 3
n_params = 0  # TODO
`}
  wrapSuffix={`
print(n_params)
`}
  hint="nin*nout + nout."
  solution={`nin, nout = 2, 3
n_params = nin * nout + nout
`}
  sourceChecks={[
    {name: 'arith', pattern: '\\*|\\+', must: true, hint: 'weights plus biases'},
  ]}
  tests={[
    {name: 'n', equals: '9'},
  ]}
/>

:::

</Exercise>

<Exercise title="11.8b.5 Epochs × steps" anchor="exercise-cp-118-epochs">

:::tip Activity: Training length
With $4$ samples and $1000$ epochs (one pass per sample per epoch), print total sample updates.

No input.

<CodeExercise
  title="Updates"
  heading="exercise-cp-118-epochs"
  lang="python"
  filename="main.py"
  prompt="4*1000."
  sampleLog={`4000`}
  starter={`n_samples, epochs = 4, 1000
updates = 0  # TODO
`}
  wrapSuffix={`
print(updates)
`}
  hint="Product."
  solution={`n_samples, epochs = 4, 1000
updates = n_samples * epochs
`}
  sourceChecks={[
    {name: 'mul', pattern: '\\*', must: true, hint: 'n_samples * epochs'},
  ]}
  tests={[
    {name: 'u', equals: '4000'},
  ]}
/>

:::

</Exercise>
</ExerciseSet>

## Write-ups

<ProblemSet id="cp-118b-assess">
  <Problem
    type="concept"
    title="11.8b.W1"
    minWords={3}
    keywords={['dense', 'layer', 'unit']}
    prompt={`What does the units argument of a Dense layer control?`}
    answer={`The dimension of that layer’s output vector (number of neurons in the layer).`}
  />
  <Problem
    type="concept"
    title="11.8b.W2"
    minWords={3}
    keywords={['compile', 'fit', 'loss']}
    prompt={`In Keras, what is the difference between compile and fit?`}
    answer={`compile sets the Loss and optimizer; fit runs the training loop for a chosen number of epochs on data.`}
  />
</ProblemSet>

Next: [11.9 Image processing with OpenCV](./11-09-opencv-image-processing).
