---
sidebar_position: 8
title: Chapter 5b - Data Structures
---

## Why Data Structures?

A **data structure** is a way of organizing data in memory so that operations on it (insert, delete, search) are efficient. Choosing the right one is one of the most important decisions in software engineering.

| Structure | Access | Insert/Delete | Search | Use when |
|---|---|---|---|---|
| Array / `vector` | O(1) | O(n) (middle) | O(n) | Random access, cache-friendly |
| Linked List | O(n) | O(1) (at known node) | O(n) | Frequent insert/delete, unknown size |
| Stack | O(1) top | O(1) | — | LIFO: undo, call stack, DFS |
| Queue | O(1) front | O(1) | — | FIFO: task queue, BFS |
| Map (`unordered_map`) | O(1) avg | O(1) avg | O(1) avg | Key-value lookup |

---

## Arrays (Fixed Size)

C-style arrays have a fixed size set at compile time.

```cpp
int scores[5] = {90, 85, 78, 92, 88};

cout << scores[2] << endl;   // 78

for (int i = 0; i < 5; i++) {
    cout << scores[i] << " ";
}
```

---

## Mini quiz — arrays

<MultipleChoice
  id="cpp-5b-arrays"
  title="Why structures, and arrays"
  questions={[
    {
      prompt: 'A data structure is mainly…',
      choices: [
        'A way to organize values in memory',
        'A compiler warning',
        'Always a class with virtual methods',
        'The same as a comment',
      ],
      answer: 0,
      why: 'Arrays, lists, stacks, and maps are different layouts and access rules.',
    },
    {
      prompt: 'A C-style array’s size is…',
      choices: [
        'Fixed at compile time',
        'Always infinite',
        'Set only by cin',
        'The same as vector',
      ],
      answer: 0,
      why: 'int scores[5] cannot grow later.',
    },
    {
      prompt: 'The first index of an array is…',
      choices: ['1', '0', '-1', 'n'],
      answer: 1,
      why: 'C++ arrays are 0-based. scores[0] is the first element.',
    },
  ]}
/>

---

## `vector` — Dynamic Array (STL)

`std::vector` is a resizable array. Prefer it over raw arrays almost always.

```cpp
#include <vector>
using namespace std;

vector<int> nums = {1, 2, 3};

nums.push_back(4);    // append
nums.push_back(5);

cout << nums.size() << endl;   // 5
cout << nums[2]     << endl;   // 3
```

### Iterating a Vector

```cpp
for (int n : nums) {
    cout << n << " ";
}
```

<!-- IMAGE PLACEHOLDER: Memory diagram of a vector -->

<details>
<summary>🧪 Vector operations</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v = {5, 2, 8, 1, 9, 3};

    sort(v.begin(), v.end());
    reverse(v.begin(), v.end());

    auto it = find(v.begin(), v.end(), 8);
    if (it != v.end()) {
        cout << "Found 8 at index " << (it - v.begin()) << endl;
    }

    cout << "Max: " << *max_element(v.begin(), v.end()) << endl;
    cout << "Min: " << *min_element(v.begin(), v.end()) << endl;

    return 0;
}
`}
/>

</details>

---

## Linked List (Manual Implementation)

A **linked list** is a chain of nodes, each holding data and a pointer to the next node.

```cpp
struct Node {
    int data;
    Node* next;

    Node(int val) : data(val), next(nullptr) {}
};
```

<!-- IMAGE PLACEHOLDER: Diagram showing linked list nodes -->

<details>
<summary>🧪 Linked list</summary>

<PistonRunner
  lang="c++"
  height="400px"
  code={`#include <iostream>
using namespace std;

struct Node {
    int data;
    Node* next;
    Node(int val) : data(val), next(nullptr) {}
};

class LinkedList {
private:
    Node* head;

public:
    LinkedList() : head(nullptr) {}

    void pushFront(int val) {
        Node* newNode = new Node(val);
        newNode->next = head;
        head = newNode;
    }

    void pushBack(int val) {
        Node* newNode = new Node(val);
        if (!head) { head = newNode; return; }
        Node* curr = head;
        while (curr->next) curr = curr->next;
        curr->next = newNode;
    }

    void popFront() {
        if (!head) return;
        Node* temp = head;
        head = head->next;
        delete temp;
    }

    void print() {
        Node* curr = head;
        while (curr) {
            cout << curr->data;
            if (curr->next) cout << " -> ";
            curr = curr->next;
        }
        cout << endl;
    }

    ~LinkedList() {
        while (head) popFront();
    }
};

int main() {
    LinkedList list;
    list.pushBack(10);
    list.pushBack(20);
    list.pushBack(30);
    list.pushFront(5);
    list.print();
    list.popFront();
    list.print();
    return 0;
}
`}
/>

</details>

---

## Mini quiz — vector and lists

<MultipleChoice
  id="cpp-5b-vector-list"
  title="vector and linked lists"
  questions={[
    {
      prompt: 'std::vector can…',
      choices: [
        'Grow at run time',
        'Never change size',
        'Only store char',
        'Replace main()',
      ],
      answer: 0,
      why: 'push_back adds elements; the array is dynamic.',
    },
    {
      prompt: 'A linked list node typically stores…',
      choices: [
        'Only an index into a vector',
        'A value and a pointer to the next node',
        'The entire program',
        'A file path',
      ],
      answer: 1,
      why: 'That pointer chain is how the list grows without a fixed array.',
    },
    {
      prompt: 'Random access (item i) is fastest in…',
      choices: ['a singly linked list', 'a vector / array', 'a queue only', 'cout'],
      answer: 1,
      why: 'Arrays sit in contiguous memory. Lists must walk node by node.',
    },
  ]}
/>

---

## Stack

A **stack** is LIFO (Last In, First Out) — like a stack of plates. Use `std::stack` from `<stack>`.

```cpp
#include <stack>

stack<int> s;
s.push(1);
s.push(2);
s.push(3);
cout << s.top() << endl;   // 3
s.pop();
```

<!-- IMAGE PLACEHOLDER: Stack diagram -->

### Application: Balanced Parentheses

<details>
<summary>🧪 Balanced parentheses</summary>

<PistonRunner
  lang="c++"
  height="340px"
  code={`#include <iostream>
#include <stack>
#include <string>
using namespace std;

bool isBalanced(string s) {
    stack<char> st;
    for (char c : s) {
        if (c == '(' || c == '[' || c == '{') {
            st.push(c);
        } else if (c == ')' || c == ']' || c == '}') {
            if (st.empty()) return false;
            char top = st.top(); st.pop();
            if ((c == ')' && top != '(') ||
                (c == ']' && top != '[') ||
                (c == '}' && top != '{')) return false;
        }
    }
    return st.empty();
}

int main() {
    cout << isBalanced("({[]})") << endl;
    cout << isBalanced("([)]")   << endl;
    cout << isBalanced("{")      << endl;
    return 0;
}
`}
/>

</details>

---

## Challenge: balanced parens

Finish `isBalanced` so it returns `yes` if every `( )` pair matches, otherwise `no`. Only round parentheses are tested.

| Input | Return |
|---|---|
| `()` | `yes` |
| `(()` | `no` |

<CodeExercise
  title="balanced parens"
  heading="Challenge: balanced parens"
  lang="c++"
  prompt="Implement isBalanced(string s) for ( and ). Print yes or no via the harness."
  sampleLog={`(input) ()
yes`}
  starter={`bool isBalanced(string s) {
    // TODO: count opens/closes or use a stack
    return false;
}
`}
  wrapPrefix={`#include <iostream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    string s;
    cin >> s;
    cout << (isBalanced(s) ? "yes" : "no") << endl;
    return 0;
}
`}
  tests={[
    { name: '() → yes', stdin: '()', equals: 'yes' },
    { name: '(() → no', stdin: '(()', equals: 'no' },
    { name: '()() → yes', stdin: '()()', equals: 'yes' },
    { name: ')( → no', stdin: ')(', equals: 'no' },
  ]}
/>

---

## Queue

A **queue** is FIFO (First In, First Out) — like a line of people. Use `std::queue` from `<queue>`.

```cpp
#include <queue>

queue<string> q;
q.push("Alice");
q.push("Bob");
cout << q.front() << endl;  // Alice
q.pop();
```

<!-- IMAGE PLACEHOLDER: Queue diagram -->

### Application: Print Queue Simulation

<details>
<summary>🧪 Print queue</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
#include <queue>
#include <string>
using namespace std;

int main() {
    queue<string> printQueue;
    printQueue.push("report.pdf");
    printQueue.push("photo.jpg");
    printQueue.push("notes.txt");

    int jobNum = 1;
    while (!printQueue.empty()) {
        cout << "Printing job " << jobNum++ << ": " << printQueue.front() << endl;
        printQueue.pop();
    }
    return 0;
}
`}
/>

</details>

---

## Map (Hash Map / Dictionary)

`std::unordered_map` stores key-value pairs with O(1) average access.

```cpp
#include <unordered_map>

unordered_map<string, int> ages;
ages["Alice"] = 22;
ages["Bob"]   = 19;
cout << ages["Alice"] << endl;  // 22
```

### Word Frequency Counter

<details>
<summary>🧪 Word frequency</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
#include <unordered_map>
#include <sstream>
#include <string>
using namespace std;

int main() {
    string text = "the cat sat on the mat the cat";
    unordered_map<string, int> freq;

    istringstream ss(text);
    string word;
    while (ss >> word) {
        freq[word]++;
    }

    for (auto& p : freq) {
        cout << p.first << ": " << p.second << endl;
    }
    return 0;
}
`}
/>

</details>

---

## Mini quiz — queue and map

<MultipleChoice
  id="cpp-5b-queue-map"
  title="Queue and map"
  questions={[
    {
      prompt: 'A queue is…',
      choices: ['LIFO', 'FIFO', 'Random only', 'Always sorted'],
      answer: 1,
      why: 'First in, first out — like a line of people.',
    },
    {
      prompt: 'A stack is…',
      choices: ['FIFO', 'LIFO', 'A sorted map', 'A pointer type'],
      answer: 1,
      why: 'Last in, first out — like a stack of plates.',
    },
    {
      prompt: 'A map stores…',
      choices: [
        'Only a single int',
        'Key–value pairs',
        'Functions but not data',
        'The call stack',
      ],
      answer: 1,
      why: 'Look up a value by its key, like a dictionary.',
    },
  ]}
/>

---

## `set` — Unique Sorted Elements

```cpp
#include <set>

set<int> s = {3, 1, 4, 1, 5, 9, 2, 6, 5};
for (int x : s) cout << x << " ";
// Output: 1 2 3 4 5 6 9
```

<details>
<summary>🧪 set</summary>

<PistonRunner
  lang="c++"
  height="220px"
  code={`#include <iostream>
#include <set>
using namespace std;

int main() {
    set<int> s = {3, 1, 4, 1, 5, 9, 2, 6, 5};
    for (int x : s) cout << x << " ";
    cout << endl;

    s.insert(7);
    s.erase(3);
    cout << "count 5: " << s.count(5) << endl;
    cout << "count 3: " << s.count(3) << endl;
    return 0;
}
`}
/>

</details>

---

## Choosing a Container

```
Do you need key-value pairs?
  YES → unordered_map (fast) or map (sorted)
  NO  →
    Do you need unique elements?
      YES → unordered_set or set
      NO  →
        Do you need LIFO?  → stack
        Do you need FIFO?  → queue
        Otherwise          → vector (default choice)
```

---

## Mini quiz — set and choosing containers

<MultipleChoice
  id="cpp-5b-set-choose"
  title="set and container choice"
  questions={[
    {
      prompt: 'A set stores…',
      choices: [
        'Duplicates in insertion order',
        'Unique elements (std::set is sorted)',
        'Only key–value pairs',
        'The heap pointer',
      ],
      answer: 1,
      why: 'Inserting 1 twice still leaves one 1. set keeps them ordered.',
    },
    {
      prompt: 'Default choice for a growable list is usually…',
      choices: ['stack', 'vector', 'map', 'queue'],
      answer: 1,
      why: 'vector is the general-purpose dynamic array.',
    },
    {
      prompt: 'Need key–value lookup? Start with…',
      choices: ['queue', 'map or unordered_map', 'a raw C array only', 'endl'],
      answer: 1,
      why: 'Maps are built for that. unordered_map is typically faster; map stays sorted by key.',
    },
  ]}
/>

---

## Exercises

<ExerciseSet>
<Exercise title="Stack with a linked list" anchor="exercise-stack-linked-list">

:::tip Activity: Implement a Stack with a Linked List
Without using `std::stack`, implement a `Stack<int>` class backed by a linked list with `push`, `pop`, `top`, and `isEmpty` methods.

<CodeExercise
  title="Stack with a linked list"
  heading="exercise-stack-linked-list"
  lang="c++"
  prompt="Implement reverseThree: print three ints from last to first (stack order). Hidden tests send three numbers."
  sampleLog={`(input) 10 20 30
30
20
10`}
  starter={`void reverseThree(int a, int b, int c) {
    // TODO: print c, then b, then a, each on its own line
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int a, b, c;
    cin >> a >> b >> c;
    reverseThree(a, b, c);
    return 0;
}
`}
  tests={[
    { name: '10 20 30', stdin: '10 20 30', equals: '30 20 10' },
    { name: '1 2 3', stdin: '1 2 3', equals: '3 2 1' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="360px"
  code={`#include <iostream>
using namespace std;

struct Node {
    int data;
    Node* next;
    Node(int v) : data(v), next(nullptr) {}
};

class Stack {
    Node* head;
public:
    Stack() : head(nullptr) {}

    void push(int v) {
        Node* n = new Node(v);
        n->next = head;
        head = n;
    }

    void pop() {
        if (!head) return;
        Node* tmp = head;
        head = head->next;
        delete tmp;
    }

    int top() { return head->data; }
    bool isEmpty() { return head == nullptr; }

    ~Stack() { while (!isEmpty()) pop(); }
};

int main() {
    Stack s;
    s.push(10); s.push(20); s.push(30);
    while (!s.isEmpty()) {
        cout << s.top() << endl;
        s.pop();
    }
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Two sum" anchor="exercise-two-sum">

:::tip Activity: Two Sum (Map)
Given a vector of integers and a target sum, find and print all pairs that add up to the target. Use an `unordered_map` for O(n) time.

<CodeExercise
  title="Two sum"
  heading="exercise-two-sum"
  lang="c++"
  prompt="Read target then print yes if 2 7 11 15 1 8 contains a pair that sums to target."
  sampleLog={`(input) 9
yes`}
  starter={`bool hasTwoSum(int target) {
    int nums[] = {2, 7, 11, 15, 1, 8};
    // TODO: return true if two values add to target
    return false;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int t;
    cin >> t;
    cout << (hasTwoSum(t) ? "yes" : "no") << endl;
    return 0;
}
`}
  tests={[
    { name: '9 → yes (2+7)', stdin: '9', equals: 'yes' },
    { name: '26 → yes (11+15)', stdin: '26', equals: 'yes' },
    { name: '3 → no', stdin: '3', equals: 'no' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

void twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> seen;
    for (int i = 0; i < nums.size(); i++) {
        int complement = target - nums[i];
        if (seen.count(complement)) {
            cout << nums[seen[complement]] << " + " << nums[i]
                 << " = " << target << endl;
        }
        seen[nums[i]] = i;
    }
}

int main() {
    vector<int> v = {2, 7, 11, 15, 1, 8};
    twoSum(v, 9);
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Ticket counter" anchor="exercise-ticket-counter">

:::tip Activity: Queue Simulation — Ticket Counter
Simulate a ticket counter with 3 service windows. People join a single queue; each window takes the next person when free. Print who is served by which window.

<CodeExercise
  title="Ticket counter"
  heading="exercise-ticket-counter"
  lang="c++"
  prompt="Implement nextWindow(int w) cycling 1 → 2 → 3 → 1."
  sampleLog={`(input) 1
2`}
  starter={`int nextWindow(int w) {
    // TODO: 1,2,3 then back to 1
    return w;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int w;
    cin >> w;
    cout << nextWindow(w) << endl;
    return 0;
}
`}
  tests={[
    { name: '1 → 2', stdin: '1', equals: '2' },
    { name: '2 → 3', stdin: '2', equals: '3' },
    { name: '3 → 1', stdin: '3', equals: '1' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
#include <queue>
#include <string>
using namespace std;

int main() {
    queue<string> line;
    line.push("Alice"); line.push("Bob");   line.push("Carol");
    line.push("Dave");  line.push("Eve");   line.push("Frank");
    line.push("Grace");

    int windows = 3;
    int w = 1;

    while (!line.empty()) {
        cout << "Window " << w << " serves " << line.front() << endl;
        line.pop();
        w = (w % windows) + 1;
    }
    return 0;
}
`}
/>

</details>
:::

</Exercise>
</ExerciseSet>
