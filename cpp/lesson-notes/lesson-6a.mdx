---
sidebar_position: 9
title: Chapter 6a - Templates & the STL
---

## Why Templates?

Without templates, you'd need a separate `max` function for every type:

```cpp
int    maxInt(int a,    int b)    { return a > b ? a : b; }
double maxDouble(double a, double b) { return a > b ? a : b; }
string maxString(string a, string b) { return a > b ? a : b; }
```

**Templates** let you write the logic once and let the compiler generate the right version for any type.

---

## Function Templates

```cpp
template <typename T>
T maxOf(T a, T b) {
    return a > b ? a : b;
}

int main() {
    cout << maxOf(3, 7)           << endl;  // 7
    cout << maxOf(3.14, 2.71)     << endl;  // 3.14
    cout << maxOf('z', 'a')       << endl;  // z
}
```

The compiler generates a separate `maxOf<int>`, `maxOf<double>`, etc. at compile time — zero runtime overhead.

### Multiple Type Parameters

```cpp
template <typename T, typename U>
void printPair(T first, U second) {
    cout << "(" << first << ", " << second << ")" << endl;
}
```

<!-- IMAGE PLACEHOLDER: Diagram showing template instantiation -->

<details>
<summary>🧪 Function templates</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
#include <string>
using namespace std;

template <typename T>
T maxOf(T a, T b) {
    return a > b ? a : b;
}

int main() {
    cout << maxOf(3, 7) << endl;
    cout << maxOf(3.14, 2.71) << endl;
    cout << maxOf('z', 'a') << endl;
    cout << maxOf(string("cat"), string("dog")) << endl;
    return 0;
}
`}
/>

</details>

---

## Challenge: max of two

Finish `maxOf` so it **returns** the larger of two ints.

| a | b | Return |
|---|---|---|
| `3` | `7` | `7` |
| `10` | `2` | `10` |

<CodeExercise
  title="max of two"
  heading="Challenge: max of two"
  lang="c++"
  prompt="Implement maxOf(int a, int b). Hidden tests send several pairs."
  sampleLog={`(input) 3 7
7`}
  starter={`int maxOf(int a, int b) {
    // TODO: return the larger value
    return a;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int a, b;
    cin >> a >> b;
    cout << maxOf(a, b) << endl;
    return 0;
}
`}
  tests={[
    { name: '3 7 → 7', stdin: '3 7', equals: '7' },
    { name: '10 2 → 10', stdin: '10 2', equals: '10' },
    { name: '4 4 → 4', stdin: '4 4', equals: '4' },
  ]}
/>

---

## Class Templates

You can template entire classes. This is how `std::vector<T>`, `std::stack<T>`, and `std::pair<T,U>` work internally.

```cpp
template <typename T>
class Box {
private:
    T value;
public:
    Box(T v) : value(v) {}
    T get() const { return value; }
    void set(T v) { value = v; }
};
```

### Template Stack (Generic)

```cpp
template <typename T>
class Stack {
    vector<T> data;
public:
    void push(T val)    { data.push_back(val); }
    void pop()          { data.pop_back(); }
    T    top()  const   { return data.back(); }
    bool empty() const  { return data.empty(); }
};
```

<details>
<summary>🧪 Template Pair</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <string>
using namespace std;

template <typename A, typename B>
class Pair {
public:
    A first;
    B second;
    Pair(A a, B b) : first(a), second(b) {}

    void print() {
        cout << "(" << first << ", " << second << ")" << endl;
    }
};

int main() {
    Pair<string, int> p("Alice", 95);
    p.print();

    Pair<double, double> coords(48.8566, 2.3522);
    coords.print();
    return 0;
}
`}
/>

</details>

---

## Template Specialization

You can provide a custom implementation for a specific type while keeping the generic version for everything else.

```cpp
template <typename T>
void describe(T val) {
    cout << "Value: " << val << endl;
}

template <>
void describe<bool>(bool val) {
    cout << "Boolean: " << (val ? "true" : "false") << endl;
}
```

<details>
<summary>🧪 Specialization</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
using namespace std;

template <typename T>
void describe(T val) {
    cout << "Value: " << val << endl;
}

template <>
void describe<bool>(bool val) {
    cout << "Boolean: " << (val ? "true" : "false") << endl;
}

int main() {
    describe(42);
    describe(3.14);
    describe(true);
    return 0;
}
`}
/>

</details>

---

## Mini quiz — class templates and specialization

<MultipleChoice
  id="cpp-6a-templates"
  title="Class templates"
  questions={[
    {
      prompt: 'What does this mean?',
      code: 'template <typename T> class Box',
      codeLang: 'cpp',
      choices: [
        'Box can hold any type T',
        'Box only holds int',
        'T must be a pointer',
        'The class cannot have methods',
      ],
      answer: 0,
      why: 'vector<int> and vector<string> are the same template with different T.',
    },
    {
      prompt: 'Template specialization is…',
      choices: [
        'Deleting the generic version',
        'A custom version for one type',
        'A slower cin',
        'Always undefined behaviour',
      ],
      answer: 1,
      why: 'describe<bool> can print true/false while other types use the generic describe.',
    },
    {
      prompt: 'std::vector is implemented as…',
      choices: ['A macro only', 'A class template', 'A single global array', 'main()'],
      answer: 1,
      why: 'The library ships a templated container you instantiate with a type.',
    },
  ]}
/>

---

## The Standard Template Library (STL)

The STL provides ready-made containers, algorithms, and iterators. Everything works generically through templates.

### STL Containers Overview

| Container | Header | Ordered? | Unique keys? | Access |
|---|---|---|---|---|
| `vector<T>` | `<vector>` | Insertion order | — | O(1) index |
| `list<T>` | `<list>` | Insertion order | — | O(1) insert |
| `deque<T>` | `<deque>` | Insertion order | — | O(1) front+back |
| `stack<T>` | `<stack>` | LIFO | — | O(1) top |
| `queue<T>` | `<queue>` | FIFO | — | O(1) front |
| `priority_queue<T>` | `<queue>` | Max-heap | — | O(log n) |
| `set<T>` | `<set>` | Sorted | Yes | O(log n) |
| `unordered_set<T>` | `<unordered_set>` | No | Yes | O(1) avg |
| `map<K,V>` | `<map>` | Sorted by key | Yes | O(log n) |
| `unordered_map<K,V>` | `<unordered_map>` | No | Yes | O(1) avg |

---

## Iterators

An **iterator** is a generalization of a pointer — it points to an element in a container and can be advanced.

```cpp
vector<int> v = {10, 20, 30, 40, 50};

for (auto it = v.begin(); it != v.end(); ++it) {
    cout << *it << " ";
}
```

<!-- IMAGE PLACEHOLDER: iterator arrows -->

---

## Mini quiz — STL and iterators

<MultipleChoice
  id="cpp-6a-iterators"
  title="STL and iterators"
  questions={[
    {
      prompt: 'The STL is…',
      choices: [
        'A set of containers, iterators, and algorithms',
        'The C preprocessor',
        'Only iostream',
        'A replacement for main',
      ],
      answer: 0,
      why: 'vector, map, sort, and friends live in the standard library templates.',
    },
    {
      prompt: 'An iterator is closest to…',
      choices: [
        'A generalized pointer into a container',
        'A destructor',
        'A header guard',
        'A float register',
      ],
      answer: 0,
      why: 'begin() points at the first element; ++it walks forward; *it is the value.',
    },
    {
      prompt: 'Where does this point?',
      code: 'v.end()',
      codeLang: 'cpp',
      choices: [
        'At the last element',
        'One past the last element',
        'At v[0]',
        'At nullptr always',
      ],
      answer: 1,
      why: 'The loop test is it != v.end(), not it <= last.',
    },
  ]}
/>

---

## STL Algorithms (`<algorithm>`)

STL algorithms work on any container through iterators.

```cpp
sort(v.begin(), v.end());
reverse(v.begin(), v.end());
int s = accumulate(v.begin(), v.end(), 0);
```

### Lambda Functions with Algorithms

A **lambda** is an anonymous function written inline.

```cpp
int evens = count_if(v.begin(), v.end(), [](int x) { return x % 2 == 0; });
```

Lambda syntax: `[capture](parameters) { body }`

| Capture | Meaning |
|---|---|
| `[]` | No captures |
| `[x]` | Capture `x` by value |
| `[&x]` | Capture `x` by reference |
| `[=]` | Capture all locals by value |
| `[&]` | Capture all locals by reference |

<details>
<summary>🧪 Full algorithm example</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
using namespace std;

int main() {
    vector<int> grades = {72, 88, 95, 60, 79, 91, 84, 55};

    double avg = accumulate(grades.begin(), grades.end(), 0.0) / grades.size();
    cout << "Average: " << avg << endl;

    int passing = count_if(grades.begin(), grades.end(),
                           [](int g) { return g >= 70; });
    cout << "Passing: " << passing << "/" << grades.size() << endl;

    sort(grades.begin(), grades.end(), greater<int>());
    cout << "Top grade: " << grades.front() << endl;
    cout << "Low grade: " << grades.back()  << endl;

    return 0;
}
`}
/>

</details>

---

## `priority_queue` — Heap

A `priority_queue` always gives you the largest (or smallest) element in O(log n).

```cpp
priority_queue<int> maxHeap;
maxHeap.push(3);
maxHeap.push(9);
cout << maxHeap.top();  // 9
```

### Application: Top K Elements

<details>
<summary>🧪 Top K</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main() {
    vector<int> data = {3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5};
    int k = 3;

    priority_queue<int, vector<int>, greater<int>> minHeap;
    for (int x : data) {
        minHeap.push(x);
        if (minHeap.size() > k) minHeap.pop();
    }

    cout << "Top " << k << " elements: ";
    while (!minHeap.empty()) {
        cout << minHeap.top() << " ";
        minHeap.pop();
    }
    cout << endl;
    return 0;
}
`}
/>

</details>

---

## Mini quiz — algorithms and heaps

<MultipleChoice
  id="cpp-6a-algo-heap"
  title="Algorithms and priority_queue"
  questions={[
    {
      prompt: 'What does this mean?',
      code: 'sort(v.begin(), v.end())',
      codeLang: 'cpp',
      choices: [
        'A pair of iterators',
        'The class name only',
        'A raw C array size of 0',
        'cin',
      ],
      answer: 0,
      why: 'Algorithms are iterator-based so they work on many containers.',
    },
    {
      prompt: 'A lambda is…',
      choices: [
        'An anonymous function written inline',
        'A kind of destructor',
        'Always slower than goto',
        'A header file',
      ],
      answer: 0,
      why: 'You pass [](const T& a, const T& b){ … } into sort.',
    },
    {
      prompt: 'priority_queue is typically a…',
      choices: ['linked list of strings', 'heap (largest or smallest on top)', 'set of files', 'map of functions'],
      answer: 1,
      why: 'top() is the highest priority; pop() removes it.',
    },
  ]}
/>

---

## Exercises

<ExerciseSet>
<Exercise title="Generic min and clamp" anchor="exercise-generic-min-clamp">

:::tip Activity: Generic `min` and `clamp`
Write a template function `myMin(a, b)` and `clamp(val, lo, hi)` that works for any comparable type. Test with `int`, `double`, and `char`.

<CodeExercise
  title="Generic min and clamp"
  heading="exercise-generic-min-clamp"
  lang="c++"
  prompt="Implement clampInt(val, lo, hi). Hidden tests send several triples."
  sampleLog={`(input) 15 0 10
10`}
  starter={`int clampInt(int val, int lo, int hi) {
    // TODO: clip val into [lo, hi]
    return val;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int val, lo, hi;
    cin >> val >> lo >> hi;
    cout << clampInt(val, lo, hi) << endl;
    return 0;
}
`}
  tests={[
    { name: '15 in [0,10] → 10', stdin: '15 0 10', equals: '10' },
    { name: '-5 in [0,10] → 0', stdin: '-5 0 10', equals: '0' },
    { name: '5 in [0,10] → 5', stdin: '5 0 10', equals: '5' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

template <typename T>
T myMin(T a, T b) {
    return a < b ? a : b;
}

template <typename T>
T clamp(T val, T lo, T hi) {
    if (val < lo) return lo;
    if (val > hi) return hi;
    return val;
}

int main() {
    cout << myMin(3, 7)         << endl;
    cout << myMin(3.14, 2.71)   << endl;
    cout << myMin('z', 'a')     << endl;

    cout << clamp(15, 0, 10)    << endl;
    cout << clamp(-5, 0, 10)    << endl;
    cout << clamp(5, 0, 10)     << endl;
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Word frequency" anchor="exercise-word-frequency">

:::tip Activity: Word Frequency with `map`
Read a sentence from the user, count word frequencies using `map<string, int>` (sorted), and print the results sorted alphabetically.

<CodeExercise
  title="Word frequency"
  heading="exercise-word-frequency"
  lang="c++"
  prompt="Read one line and print how many words it has (split on spaces)."
  sampleLog={`(input) The cat sat
3`}
  starter={`int wordCount(string line) {
    // TODO: count space-separated words
    return 0;
}
`}
  wrapPrefix={`#include <iostream>
#include <sstream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    string line;
    getline(cin, line);
    cout << wordCount(line) << endl;
    return 0;
}
`}
  tests={[
    { name: 'three words', stdin: 'The cat sat', equals: '3' },
    { name: 'one word', stdin: 'hello', equals: '1' },
    { name: 'four words', stdin: 'a b c d', equals: '4' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

Type a sentence and press Enter. Example: `The cat sat on the mat`

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <map>
#include <sstream>
#include <string>
#include <cctype>
using namespace std;

int main() {
    string line;
    cout << "Enter a sentence: " << flush;
    getline(cin, line);

    map<string, int> freq;
    istringstream ss(line);
    string word;
    while (ss >> word) {
        for (char& c : word) c = tolower(c);
        freq[word]++;
    }

    for (auto& p : freq) {
        cout << p.first << ": " << p.second << endl;
    }
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Sort students by GPA" anchor="exercise-sort-students-gpa">

:::tip Activity: Sort Students by GPA
Use `sort` with a lambda to sort a vector of `Student` objects (from Ch.4a) by GPA descending, then print the ranked list.

<CodeExercise
  title="Sort students by GPA"
  heading="exercise-sort-students-gpa"
  lang="c++"
  prompt="Implement gpaBetter(double a, double b) returning yes if a should rank above b (strictly greater GPA)."
  sampleLog={`(input) 3.9 3.2
yes`}
  starter={`string gpaBetter(double a, double b) {
    // TODO: yes if a > b
    return "no";
}
`}
  wrapPrefix={`#include <iostream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    double a, b;
    cin >> a >> b;
    cout << gpaBetter(a, b) << endl;
    return 0;
}
`}
  tests={[
    { name: '3.9 vs 3.2 → yes', stdin: '3.9 3.2', equals: 'yes' },
    { name: '3.2 vs 3.9 → no', stdin: '3.2 3.9', equals: 'no' },
    { name: '3.5 vs 3.5 → no', stdin: '3.5 3.5', equals: 'no' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="320px"
  code={`#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

struct Student {
    string name;
    double gpa;
};

int main() {
    vector<Student> students = {
        {"Alice", 3.8},
        {"Bob",   3.2},
        {"Carol", 3.9},
        {"Dave",  3.5},
    };

    sort(students.begin(), students.end(),
         [](const Student& a, const Student& b) {
             return a.gpa > b.gpa;
         });

    int rank = 1;
    for (auto& s : students) {
        cout << rank++ << ". " << s.name << " — " << s.gpa << endl;
    }
    return 0;
}
`}
/>

</details>
:::

</Exercise>
</ExerciseSet>
