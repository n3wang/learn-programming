---
sidebar_position: 3
title: Chapter 2a - Conditionals & Loops
---

## If / Else

Use `if` to run code only when a condition is true. `else if` and `else` handle alternative cases.

```cpp
int score = 75;

if (score >= 90) {
    cout << "Grade: A" << endl;
} else if (score >= 80) {
    cout << "Grade: B" << endl;
} else if (score >= 70) {
    cout << "Grade: C" << endl;
} else {
    cout << "Grade: F" << endl;
}
```

<!-- IMAGE PLACEHOLDER: Flowchart showing if → else if → else branching with the grade conditions, arrows pointing to each grade output -->

### Comparison Operators

| Operator | Meaning | Example |
|---|---|---|
| `==` | Equal to | `a == b` |
| `!=` | Not equal to | `a != b` |
| `<` | Less than | `a < b` |
| `>` | Greater than | `a > b` |
| `<=` | Less than or equal | `a <= b` |
| `>=` | Greater than or equal | `a >= b` |

:::note `=` vs `==`
`=` **assigns** a value. `==` **compares** two values. Mixing them up is one of the most common C++ bugs.
:::

### Logical Operators

Combine multiple conditions with `&&` (AND), `||` (OR), `!` (NOT).

```cpp
int age = 20;
bool hasId = true;

if (age >= 18 && hasId) {
    cout << "Access granted." << endl;
}

bool isWeekend = false;
bool isHoliday = true;

if (isWeekend || isHoliday) {
    cout << "No school today!" << endl;
}
```

<details>
<summary>🧪 Try conditionals</summary>

Press **Run**, then type a number and press **Enter**.

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int main() {
    int num;
    cout << "Enter a number: " << flush;
    cin >> num;

    if (num > 0) {
        cout << num << " is positive." << endl;
    } else if (num < 0) {
        cout << num << " is negative." << endl;
    } else {
        cout << "The number is zero." << endl;
    }

    return 0;
}
`}
/>

</details>

## Challenge: sign of a number

Finish `signWord` so it **returns** `"positive"`, `"negative"`, or `"zero"`. Use **`if` / `else if` / `else`**, not `switch`.

| Input | Return |
|---|---|
| `5` | `positive` |
| `-3` | `negative` |
| `0` | `zero` |

<CodeExercise
  title="if/else signWord"
  heading="Challenge: sign of a number"
  lang="c++"
  prompt="Implement signWord(int n) with if / else if / else and return. Do not use switch. Hidden tests send several integers."
  sampleLog={`(input) -3
negative`}
  starter={`string signWord(int n) {
    // TODO: compare n with 0, then return a word
    return "zero";
}
`}
  wrapPrefix={`#include <iostream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    cout << signWord(n) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses if', pattern: '\\bif\\s*\\(', must: true, hint: 'if (...)' },
    { name: 'Uses else', pattern: '\\belse\\b', must: true, hint: 'else' },
    { name: 'No switch', pattern: 'switch\\s*\\(', must: false, hint: 'switch (...)' },
  ]}
  tests={[
    { name: '5 → positive', stdin: '5', equals: 'positive' },
    { name: '-3 → negative', stdin: '-3', equals: 'negative' },
    { name: '0 → zero', stdin: '0', equals: 'zero' },
    { name: '1 → positive', stdin: '1', equals: 'positive' },
  ]}
/>

---

## Switch Statement

`switch` is a cleaner alternative to long `if/else if` chains when checking a single variable against exact values.

```cpp
int day = 3;

switch (day) {
    case 1:
        cout << "Monday" << endl;
        break;
    case 2:
        cout << "Tuesday" << endl;
        break;
    case 3:
        cout << "Wednesday" << endl;
        break;
    default:
        cout << "Another day" << endl;
        break;
}
```

:::note Don't forget `break`!
Without `break`, execution "falls through" to the next case. This is almost always a bug unless intentional.
:::

<details>
<summary>🧪 Try switch</summary>

Enter a day number 1–7.

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

int main() {
    int day;
    cout << "Enter a day number (1-7): " << flush;
    cin >> day;

    switch (day) {
        case 1: cout << "Monday" << endl; break;
        case 2: cout << "Tuesday" << endl; break;
        case 3: cout << "Wednesday" << endl; break;
        case 4: cout << "Thursday" << endl; break;
        case 5: cout << "Friday" << endl; break;
        case 6: cout << "Saturday" << endl; break;
        case 7: cout << "Sunday" << endl; break;
        default: cout << "Another day" << endl; break;
    }

    return 0;
}
`}
/>

</details>

## Challenge: drink menu

Finish `drinkName` so it **returns** a string from an integer order. Use **`switch`**, not `if`. A hidden `main` reads one number and prints your return value.

| Input | Return |
|---|---|
| `1` | `coffee` |
| `2` | `tea` |
| `3` | `water` |
| anything else | `unknown` |

<CodeExercise
  title="switch drinkName"
  heading="Challenge: drink menu"
  lang="c++"
  prompt="Implement drinkName(int choice) with switch/case/default and return. Do not use if. Hidden tests send several integers."
  sampleLog={`(input) 2
tea`}
  starter={`string drinkName(int choice) {
    // TODO: cases 1 coffee, 2 tea, 3 water, else unknown
    return "unknown";
}
`}
  wrapPrefix={`#include <iostream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int choice;
    cin >> choice;
    cout << drinkName(choice) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses switch', pattern: 'switch\\s*\\(', must: true, hint: 'switch (...)' },
    { name: 'No if', pattern: '\\bif\\s*\\(', must: false, hint: 'if (...)' },
  ]}
  tests={[
    { name: '1 → coffee', stdin: '1', equals: 'coffee' },
    { name: '2 → tea', stdin: '2', equals: 'tea' },
    { name: '3 → water', stdin: '3', equals: 'water' },
    { name: '9 → unknown', stdin: '9', equals: 'unknown' },
    { name: '0 → unknown', stdin: '0', equals: 'unknown' },
  ]}
/>

---

## For Loop

Use a `for` loop when you know how many times to repeat.

```cpp
// Syntax: for (init; condition; update)
for (int i = 0; i < 5; i++) {
    cout << "i = " << i << endl;
}
```

```
i = 0
i = 1
i = 2
i = 3
i = 4
```

<!-- IMAGE PLACEHOLDER: Diagram labeling the three parts of the for loop header: "int i = 0" (initialization), "i < 5" (condition), "i++" (update), with arrows showing the loop cycle -->

<details>
<summary>🧪 Loop examples</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int main() {
    // Print even numbers from 2 to 10
    for (int i = 2; i <= 10; i += 2) {
        cout << i << " ";
    }
    cout << endl;

    // Count down
    for (int i = 5; i >= 1; i--) {
        cout << i << "... ";
    }
    cout << "Blast off!" << endl;

    return 0;
}
`}
/>

</details>

## Challenge: sum with for

Finish `sumTo` so it **returns** `1 + 2 + … + n`. Use a **`for`** loop, not `while`.

| Input | Return |
|---|---|
| `5` | `15` |
| `1` | `1` |
| `10` | `55` |

<CodeExercise
  title="for sumTo"
  heading="Challenge: sum with for"
  lang="c++"
  prompt="Implement sumTo(int n) with a for loop and return the total. Do not use while. Hidden tests send several n values."
  sampleLog={`(input) 5
15`}
  starter={`int sumTo(int n) {
    int total = 0;
    // TODO: add 1, 2, ... n into total
    return total;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    cout << sumTo(n) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses for', pattern: '\\bfor\\s*\\(', must: true, hint: 'for (...)' },
    { name: 'No while', pattern: '\\bwhile\\s*\\(', must: false, hint: 'while (...)' },
  ]}
  tests={[
    { name: '5 → 15', stdin: '5', equals: '15' },
    { name: '1 → 1', stdin: '1', equals: '1' },
    { name: '10 → 55', stdin: '10', equals: '55' },
    { name: '3 → 6', stdin: '3', equals: '6' },
  ]}
/>

### Nested For Loops

Loops can be placed inside other loops. The inner loop completes fully for each step of the outer loop.

```cpp
for (int row = 1; row <= 3; row++) {
    for (int col = 1; col <= 3; col++) {
        cout << row << "x" << col << "=" << row * col << "  ";
    }
    cout << endl;
}
```

```
1x1=1  1x2=2  1x3=3
2x1=2  2x2=4  2x3=6
3x1=3  3x2=6  3x3=9
```

<details>
<summary>🧪 Nested loops</summary>

<PistonRunner
  lang="c++"
  height="240px"
  code={`#include <iostream>
using namespace std;

int main() {
    for (int row = 1; row <= 3; row++) {
        for (int col = 1; col <= 3; col++) {
            cout << row << "x" << col << "=" << row * col << "  ";
        }
        cout << endl;
    }
    return 0;
}
`}
/>

</details>

---

## While Loop

Use `while` when you want to repeat as long as a condition holds, and you don't know the count upfront.

```cpp
int count = 0;
while (count < 5) {
    cout << "count = " << count << endl;
    count++;
}
```

<!-- IMAGE PLACEHOLDER: Flowchart: check condition → true: run body + update → loop back → false: exit -->

<details>
<summary>🧪 While loop with user input</summary>

Guess until you get **42**. Type a number and press Enter after each prompt.

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

int main() {
    int guess = -1;
    int secret = 42;

    while (guess != secret) {
        cout << "Guess the number: " << flush;
        cin >> guess;

        if (guess < secret) {
            cout << "Too low!" << endl;
        } else if (guess > secret) {
            cout << "Too high!" << endl;
        }
    }

    cout << "Correct! The number was " << secret << "." << endl;
    return 0;
}
`}
/>

</details>

## Challenge: countdown with while

Finish `countDown` so it prints `n` down to `1`, separated by spaces. Use **`while`**, not `for`.

| Input | Output |
|---|---|
| `5` | `5 4 3 2 1` |
| `1` | `1` |

<CodeExercise
  title="while countDown"
  heading="Challenge: countdown with while"
  lang="c++"
  prompt="Implement countDown(int n) with a while loop. Print each number and a space. Do not use for. Hidden tests send several n values."
  sampleLog={`(input) 5
5 4 3 2 1`}
  starter={`void countDown(int n) {
    // TODO: print n, then n-1, ... down to 1, each followed by a space
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    countDown(n);
    cout << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses while', pattern: '\\bwhile\\s*\\(', must: true, hint: 'while (...)' },
    { name: 'No for', pattern: '\\bfor\\s*\\(', must: false, hint: 'for (...)' },
  ]}
  tests={[
    { name: '5 → 5 4 3 2 1', stdin: '5', equals: '5 4 3 2 1' },
    { name: '1 → 1', stdin: '1', equals: '1' },
    { name: '3 → 3 2 1', stdin: '3', equals: '3 2 1' },
  ]}
/>

---

## Do-While Loop

`do-while` always runs the body **at least once**, then checks the condition.

```cpp
int n;
do {
    cout << "Enter a positive number: ";
    cin >> n;
} while (n <= 0);

cout << "You entered: " << n << endl;
```

| Loop type | Checks condition | Use when |
|---|---|---|
| `for` | Before each iteration | Count is known |
| `while` | Before each iteration | Count is unknown |
| `do-while` | After each iteration | Body must run at least once |

<details>
<summary>🧪 Try do-while</summary>

Enter a non-positive number first (like `-3`), then a positive one.

<PistonRunner
  lang="c++"
  height="240px"
  code={`#include <iostream>
using namespace std;

int main() {
    int n;
    do {
        cout << "Enter a positive number: " << flush;
        cin >> n;
    } while (n <= 0);

    cout << "You entered: " << n << endl;
    return 0;
}
`}
/>

</details>

## Challenge: first positive with do-while

Finish `firstPositive` so it **reads integers** until one is **greater than 0**, then **returns** that value. Use **`do` / `while`**. Hidden tests send several lines of input.

| Input lines | Return |
|---|---|
| `-2` then `0` then `4` | `4` |
| `7` | `7` |

<CodeExercise
  title="do-while firstPositive"
  heading="Challenge: first positive with do-while"
  lang="c++"
  prompt="Implement firstPositive() with do { cin >> x; } while (x <= 0); then return x. Hidden tests send one or more integers."
  sampleLog={`(input) -2
0
4
4`}
  starter={`int firstPositive() {
    int x = 0;
    // TODO: read x from cin, repeat until x is greater than 0
    return x;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    cout << firstPositive() << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses do', pattern: '\\bdo\\b', must: true, hint: 'do { ... }' },
    { name: 'Uses while', pattern: '\\bwhile\\s*\\(', must: true, hint: 'while (...)' },
  ]}
  tests={[
    { name: '-2, 0, 4 → 4', stdin: '-2\n0\n4', equals: '4' },
    { name: '7 → 7', stdin: '7', equals: '7' },
    { name: '0 then 1 → 1', stdin: '0\n1', equals: '1' },
    { name: '-5 then 12 → 12', stdin: '-5\n12', equals: '12' },
  ]}
/>

---

## Break and Continue

`break` exits the loop immediately. `continue` skips the rest of the current iteration and moves to the next.

```cpp
// Skip multiples of 3, stop at 10
for (int i = 1; i <= 15; i++) {
    if (i == 10) break;
    if (i % 3 == 0) continue;
    cout << i << " ";
}
// Output: 1 2 4 5 7 8
```

<details>
<summary>🧪 Break and continue</summary>

<PistonRunner
  lang="c++"
  height="220px"
  code={`#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 15; i++) {
        if (i == 10) break;
        if (i % 3 == 0) continue;
        cout << i << " ";
    }
    cout << endl;
    return 0;
}
`}
/>

</details>

## Challenge: skip threes, then break

Finish `printSkipThrees`. Loop `i` from `1` to `20`. **`break`** when `i` equals `limit`. **`continue`** when `i` is a multiple of `3`. Print the other numbers with a space after each.

| Input (`limit`) | Output |
|---|---|
| `10` | `1 2 4 5 7 8` |
| `5` | `1 2 4` |

<CodeExercise
  title="break and continue"
  heading="Challenge: skip threes, then break"
  lang="c++"
  prompt="Implement printSkipThrees(int limit) with for, break, and continue. Hidden tests send several limit values."
  sampleLog={`(input) 10
1 2 4 5 7 8`}
  starter={`void printSkipThrees(int limit) {
    // TODO: i from 1 to 20; stop at limit; skip multiples of 3; print the rest
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int limit;
    cin >> limit;
    printSkipThrees(limit);
    cout << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses break', pattern: '\\bbreak\\b', must: true, hint: 'break' },
    { name: 'Uses continue', pattern: '\\bcontinue\\b', must: true, hint: 'continue' },
  ]}
  tests={[
    { name: '10 → 1 2 4 5 7 8', stdin: '10', equals: '1 2 4 5 7 8' },
    { name: '5 → 1 2 4', stdin: '5', equals: '1 2 4' },
    { name: '4 → 1 2', stdin: '4', equals: '1 2' },
    { name: '2 → 1', stdin: '2', equals: '1' },
  ]}
/>

---

## Exercises

<ExerciseSet>
<Exercise title="FizzBuzz" anchor="exercise-fizzbuzz">

:::tip Activity: FizzBuzz
Print the numbers 1 to 100. For multiples of 3 print "Fizz", for multiples of 5 print "Buzz", for multiples of both print "FizzBuzz".

```
1
2
Fizz
4
Buzz
Fizz
7
...
```

<CodeExercise
  title="FizzBuzz"
  heading="exercise-fizzbuzz"
  lang="c++"
  height="300px"
  prompt="Print 1 to 100, each on its own line. Multiples of 3 → Fizz, multiples of 5 → Buzz, multiples of both → FizzBuzz."
  sampleLog={`1
2
Fizz
4
Buzz
Fizz
7
...`}
  starter={`void fizzBuzz() {
    // TODO: loop 1 to 100 and print the right line each time
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    fizzBuzz();
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses a loop', pattern: '\\b(for|while)\\s*\\(', must: true, hint: 'for or while' },
  ]}
  tests={[
    { name: 'Starts 1, 2, Fizz, 4, Buzz', includes: ['1 2 fizz 4 buzz fizz 7'] },
    { name: '15 is FizzBuzz', includes: ['14 fizzbuzz 16'] },
    { name: '99 is Fizz, 100 is Buzz', includes: ['97 98 fizz buzz'] },
  ]}
/>

<details>
<summary>💡 Hint</summary>

Check `i % 3 == 0 && i % 5 == 0` first (before checking each individually), or the combined case will never be reached.

</details>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 100; i++) {
        if (i % 3 == 0 && i % 5 == 0) {
            cout << "FizzBuzz" << endl;
        } else if (i % 3 == 0) {
            cout << "Fizz" << endl;
        } else if (i % 5 == 0) {
            cout << "Buzz" << endl;
        } else {
            cout << i << endl;
        }
    }
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Multiplication table" anchor="exercise-multiplication-table">

:::tip Activity: Multiplication Table
Ask the user for a number and print its multiplication table from 1 to 10.

Expected output for input `7`:
```
7 x 1 = 7
7 x 2 = 14
...
7 x 10 = 70
```

<CodeExercise
  title="Multiplication table"
  heading="exercise-multiplication-table"
  lang="c++"
  prompt="Read n and print n x 1 = … through n x 10 = …. Hidden tests send different n values."
  sampleLog={`(input) 7
7 x 1 = 7
7 x 2 = 14
...
7 x 10 = 70`}
  starter={`void timesTable(int n) {
    // TODO: print 10 lines: n x i = n * i
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    timesTable(n);
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses a loop', pattern: '\\b(for|while)\\s*\\(', must: true, hint: 'for or while' },
  ]}
  tests={[
    { name: '7 × 1 and 7 × 10', stdin: '7', includes: ['7 x 1 = 7', '7 x 10 = 70'] },
    { name: '3 × 5 = 15', stdin: '3', includes: ['3 x 5 = 15'] },
    { name: '10 × 10 = 100', stdin: '10', includes: ['10 x 10 = 100'] },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter a number: " << flush;
    cin >> n;

    for (int i = 1; i <= 10; i++) {
        cout << n << " x " << i << " = " << n * i << endl;
    }

    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Sum of digits" anchor="exercise-sum-of-digits">

:::tip Activity: Sum of Digits
Read a positive integer from the user and print the sum of its digits.

Example: input `1234` → output `Sum of digits: 10`

<CodeExercise
  title="Sum of digits"
  heading="exercise-sum-of-digits"
  lang="c++"
  prompt="Read a positive integer and print Sum of digits: followed by the digit total. Hidden tests send several numbers."
  sampleLog={`(input) 1234
Sum of digits: 10`}
  starter={`void printDigitSum(int num) {
    // TODO: add each digit, then print Sum of digits: and the total
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int num;
    cin >> num;
    printDigitSum(num);
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses a loop', pattern: '\\b(for|while)\\s*\\(', must: true, hint: 'for or while' },
  ]}
  tests={[
    { name: '1234 → 10', stdin: '1234', includes: ['sum of digits: 10'] },
    { name: '7 → 7', stdin: '7', includes: ['sum of digits: 7'] },
    { name: '100 → 1', stdin: '100', includes: ['sum of digits: 1'] },
    { name: '999 → 27', stdin: '999', includes: ['sum of digits: 27'] },
  ]}
/>

<details>
<summary>💡 Hint</summary>

Use `% 10` to get the last digit, then `/= 10` to remove it. Loop until the number is 0.

</details>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
using namespace std;

int main() {
    int num;
    cout << "Enter a positive integer: " << flush;
    cin >> num;

    int sum = 0;
    while (num > 0) {
        sum += num % 10;
        num /= 10;
    }

    cout << "Sum of digits: " << sum << endl;
    return 0;
}
`}
/>

</details>
:::

</Exercise>
</ExerciseSet>
