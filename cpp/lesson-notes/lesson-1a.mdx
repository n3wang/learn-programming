---
sidebar_position: 2
title: Chapter 1a - Variables & Printing
---

## Your First C++ Program

Every C++ program needs two things: the `iostream` header (for input/output) and a `main()` function.

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}
```

| Part | What it does |
|---|---|
| `#include <iostream>` | Loads the input/output library |
| `using namespace std;` | Lets you write `cout` instead of `std::cout` |
| `int main()` | Entry point — where the program starts |
| `cout <<` | Prints to the screen |
| `endl` | Ends the line (like pressing Enter) |
| `return 0;` | Tells the OS the program finished successfully |

<details>
<summary>🧪 Try it yourself</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    cout << "My name is C++!" << endl;
    return 0;
}
`}
/>

**Challenge:** Change the messages to print your own name and a fun fact about yourself.

</details>

---

## Variables and Data Types

A variable stores a value in memory. In C++ you must declare the **type** of data before using a variable.

```cpp
int age = 17;
double price = 9.99;
string name = "Alice";
bool isStudent = true;
char grade = 'A';
```

<!-- IMAGE PLACEHOLDER: Table or visual showing each C++ data type (int, double, string, bool, char) with an example value and a description of what kind of data it holds -->

### Common Data Types

| Type | What it stores | Example |
|---|---|---|
| `int` | Whole numbers | `42`, `-5`, `0` |
| `double` | Decimal numbers | `3.14`, `-0.5` |
| `string` | Text (requires `<string>` or `<iostream>`) | `"Hello"` |
| `bool` | True or false | `true`, `false` |
| `char` | A single character | `'A'`, `'9'` |

<details>
<summary>🧪 Variables in action</summary>

<PistonRunner
  lang="c++"
  height="320px"
  code={`#include <iostream>
#include <string>
using namespace std;

int main() {
    int age = 17;
    double gpa = 3.85;
    string name = "Alice";
    bool isStudent = true;

    cout << "Name: " << name << endl;
    cout << "Age: " << age << endl;
    cout << "GPA: " << gpa << endl;
    cout << "Student: " << isStudent << endl;

    return 0;
}
`}
/>

</details>

---

## Mini quiz — programs and types

<MultipleChoice
  id="cpp-1a-program-types"
  title="Programs and types"
  questions={[
    {
      prompt: 'Where does a C++ program start running?',
      code: 'int main() {',
      codeLang: 'cpp',
      choices: ['iostream', 'int main()', 'using namespace std;', 'return 0;'],
      answer: 1,
      why: 'main is the entry point. The OS calls it when the program launches.',
    },
    {
      prompt: 'Which type stores a whole number with no decimal part?',
      choices: ['double', 'string', 'int', 'bool'],
      answer: 2,
      why: 'int holds integers. double holds decimals.',
    },
    {
      prompt: 'What does this do?',
      code: '#include <iostream>',
      codeLang: 'cpp',
      choices: [
        'Declares main()',
        'Loads the input/output library',
        'Prints Hello, World!',
        'Ends the program',
      ],
      answer: 1,
      why: 'The preprocessor pulls in the iostream header so cout and cin are available.',
    },
  ]}
/>

---

## Printing with `cout`

Use `<<` to chain multiple values in one `cout` statement.

```cpp
string name = "Alice";
int age = 17;

cout << "My name is " << name << " and I am " << age << " years old." << endl;
```

```
My name is Alice and I am 17 years old.
```

You can also use `"\n"` instead of `endl` — it is faster for large outputs.

```cpp
cout << "Line 1\n";
cout << "Line 2\n";
```

---

## Arithmetic Operators

```cpp
int a = 10;
int b = 3;

cout << a + b << endl;   // 13  — addition
cout << a - b << endl;   // 7   — subtraction
cout << a * b << endl;   // 30  — multiplication
cout << a / b << endl;   // 3   — integer division (truncates)
cout << a % b << endl;   // 1   — modulo (remainder)
```

:::note Integer Division
`10 / 3` in C++ gives `3`, not `3.33...` because both operands are `int`. Use `double` if you need decimals:
```cpp
double result = 10.0 / 3;   // 3.333...
```
:::

<details>
<summary>🧪 Arithmetic practice</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

int main() {
    int apples = 24;
    int friends = 5;

    int each = apples / friends;
    int leftover = apples % friends;

    cout << "Each friend gets " << each << " apples." << endl;
    cout << "There are " << leftover << " apples left over." << endl;

    return 0;
}
`}
/>

</details>

---

## Challenge: leftover apples

Finish `leftover` so it **returns** how many apples remain after sharing equally (`apples % friends`).

| apples | friends | Return |
|---|---|---|
| `24` | `5` | `4` |
| `10` | `3` | `1` |

<CodingExam
  title="leftover apples"
  heading="Challenge: leftover apples"
  lang="c++"
  prompt="Implement leftover(int apples, int friends) using % and return the remainder. Hidden tests send several pairs."
  sampleLog={`(input) 24 5
4`}
  starter={`int leftover(int apples, int friends) {
    // TODO: return the remainder after equal shares
    return 0;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int apples, friends;
    cin >> apples >> friends;
    cout << leftover(apples, friends) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses %', pattern: '%', must: true, hint: 'apples % friends' },
  ]}
  tests={[
    { name: '24 and 5 → 4', stdin: '24 5', equals: '4' },
    { name: '10 and 3 → 1', stdin: '10 3', equals: '1' },
    { name: '9 and 3 → 0', stdin: '9 3', equals: '0' },
  ]}
/>

---

## User Input with `cin`

`cin >>` reads a value and stores it in a variable.

🧪 **Try it:** press **Run**, wait until you see `Enter your age:`, type a number in the box under the output, then press **Enter**.

<PistonRunner
  lang="c++"
  height="240px"
  code={`#include <iostream>
using namespace std;

int main() {
    int age;
    cout << "Enter your age: " << flush;
    cin >> age;
    cout << "You are " << age << " years old." << endl;
    return 0;
}
`}
/>

**Challenge:** Change the program so it also asks for your favorite number and prints both values. After each prompt, type a value and press Enter.

:::note Reading strings with spaces
`cin >>` stops at a space. Use `getline(cin, variable)` to read a full line:
```cpp
string fullName;
cout << "Enter your full name: ";
getline(cin, fullName);
cout << "Hello, " << fullName << "!" << endl;
```
:::

<details>
<summary>🧪 Input example</summary>

<PistonRunner
  lang="c++"
  height="340px"
  code={`#include <iostream>
#include <string>
using namespace std;

int main() {
    string name;
    int birthYear;
    int currentYear = 2025;

    cout << "What is your name? " << flush;
    getline(cin, name);

    cout << "What year were you born? " << flush;
    cin >> birthYear;

    int age = currentYear - birthYear;
    cout << "Hello, " << name << "! You are approximately " << age << " years old." << endl;

    return 0;
}
`}
/>

</details>

---

## Exercises

<ExerciseSet>
<Exercise title="Temperature converter" anchor="exercise-temperature-converter">

:::tip Activity: Temperature Converter
Write a C++ program that asks the user for a temperature in Celsius and prints the Fahrenheit equivalent.

Formula: `F = (C × 9/5) + 32`

Expected output:
```
Enter temperature in Celsius: 100
100 Celsius = 212 Fahrenheit
```

<CodingExam
  title="Temperature converter"
  heading="exercise-temperature-converter"
  lang="c++"
  prompt="Read a Celsius temperature and print the Fahrenheit equivalent using F = (C × 9/5) + 32. Hidden tests send several temperatures."
  sampleLog={`Enter temperature in Celsius: 100
100 Celsius = 212 Fahrenheit`}
  starter={`#include <iostream>
using namespace std;

int main() {
    // TODO: read Celsius with cin
    // TODO: compute Fahrenheit
    // TODO: print the result
    return 0;
}
`}
  tests={[
    { name: 'Boiling water (100°C)', stdin: '100', includes: ['212'] },
    { name: 'Freezing (0°C)', stdin: '0', includes: ['32'] },
    { name: 'Body temp (37°C)', stdin: '37', includes: ['98'] },
  ]}
/>

<details>
<summary>💡 Hint</summary>

Use `double` for the temperature variables so you get decimal precision. Read the Celsius value with `cin`, compute the formula, then print with `cout`.

</details>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int main() {
    double celsius;
    cout << "Enter temperature in Celsius: " << flush;
    cin >> celsius;

    double fahrenheit = (celsius * 9.0 / 5.0) + 32;
    cout << celsius << " Celsius = " << fahrenheit << " Fahrenheit" << endl;

    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Area calculator" anchor="exercise-area-calculator">

:::tip Activity: Area Calculator
Write a program that reads the length and width of a rectangle from the user and prints the area and perimeter.

Expected output:
```
Enter length: 5
Enter width: 3
Area: 15
Perimeter: 16
```

<CodingExam
  title="Area calculator"
  heading="exercise-area-calculator"
  lang="c++"
  prompt="Read length then width. Print Area: and Perimeter:. Hidden tests send several pairs."
  sampleLog={`Enter length: 5
Enter width: 3
Area: 15
Perimeter: 16`}
  starter={`#include <iostream>
using namespace std;

int main() {
    // TODO: read length and width, print area and perimeter
    return 0;
}
`}
  tests={[
    { name: '5 by 3', stdin: '5\n3', includes: ['area: 15', 'perimeter: 16'] },
    { name: '2 by 2', stdin: '2\n2', includes: ['area: 4', 'perimeter: 8'] },
    { name: '10 by 1', stdin: '10\n1', includes: ['area: 10', 'perimeter: 22'] },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
using namespace std;

int main() {
    double length, width;

    cout << "Enter length: " << flush;
    cin >> length;

    cout << "Enter width: " << flush;
    cin >> width;

    cout << "Area: " << length * width << endl;
    cout << "Perimeter: " << 2 * (length + width) << endl;

    return 0;
}
`}
/>

</details>
:::

</Exercise>
</ExerciseSet>
