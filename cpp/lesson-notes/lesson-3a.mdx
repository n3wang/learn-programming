---
sidebar_position: 4
title: Chapter 3a - Functions
---

## What Is a Function?

A function is a named, reusable block of code. Instead of writing the same logic in multiple places, you write it once inside a function and call it whenever needed.

```cpp
// Function definition
void greet() {
    cout << "Hello from a function!" << endl;
}

int main() {
    greet();   // Function call
    greet();   // Call it again
    return 0;
}
```

<!-- IMAGE PLACEHOLDER: Diagram showing a function as a black box — input (parameters) goes in, some work happens inside, output (return value) comes out -->

---

## Function Anatomy

```cpp
returnType functionName(parameterType paramName, ...) {
    // body
    return value;  // required if returnType is not void
}
```

| Part | Example | Meaning |
|---|---|---|
| Return type | `int`, `double`, `void` | Type of value the function sends back |
| Function name | `calculateArea` | What you call to run the function |
| Parameters | `double length, double width` | Input values the function receives |
| Return statement | `return length * width;` | Sends a result back to the caller |

---

## Mini quiz — functions

<MultipleChoice
  id="cpp-3a-anatomy"
  title="What is a function?"
  questions={[
    {
      prompt: 'A function is mainly…',
      choices: [
        'A named block of code you can call',
        'A type of loop',
        'A header file',
        'A compiler error',
      ],
      answer: 0,
      why: 'You write it once and call it whenever you need that work done.',
    },
    {
      prompt: 'If the return type is void, you…',
      choices: [
        'Must return an int',
        'Must return a string',
        'Do not return a value',
        'Cannot have parameters',
      ],
      answer: 2,
      why: 'void means the function does work (often printing) but sends nothing back.',
    },
    {
      prompt: 'In this definition, a and b are…',
      code: 'int add(int a, int b)',
      codeLang: 'cpp',
      choices: ['arguments only', 'parameters', 'return types', 'namespaces'],
      answer: 1,
      why: 'Parameters are the names in the definition. Arguments are the values you pass in a call.',
    },
  ]}
/>

---

## `void` Functions (No Return Value)

Use `void` when the function performs an action but doesn't need to send a value back.

```cpp
#include <iostream>
using namespace std;

void printSeparator() {
    cout << "--------------------" << endl;
}

void greet(string name) {
    cout << "Hello, " << name << "!" << endl;
}

int main() {
    printSeparator();
    greet("Alice");
    greet("Bob");
    printSeparator();
    return 0;
}
```

<details>
<summary>🧪 Try it</summary>

<PistonRunner
  lang="c++"
  height="300px"
  code={`#include <iostream>
#include <string>
using namespace std;

void printBox(string message) {
    int len = message.length() + 4;
    for (int i = 0; i < len; i++) cout << "*";
    cout << endl;
    cout << "* " << message << " *" << endl;
    for (int i = 0; i < len; i++) cout << "*";
    cout << endl;
}

int main() {
    printBox("C++ is great");
    printBox("Hello");
    return 0;
}
`}
/>

</details>

---

## Functions That Return Values

```cpp
int add(int a, int b) {
    return a + b;
}

double circleArea(double radius) {
    return 3.14159 * radius * radius;
}

bool isEven(int n) {
    return n % 2 == 0;
}
```

The returned value can be stored in a variable, printed directly, or used in an expression:

```cpp
int sum = add(3, 7);             // store it
cout << add(10, 20) << endl;     // print it directly
cout << circleArea(5.0) << endl; // use in expression
```

<details>
<summary>🧪 Return value examples</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int max(int a, int b) {
    if (a > b) return a;
    return b;
}

double average(double a, double b, double c) {
    return (a + b + c) / 3.0;
}

int main() {
    cout << "Max of 4 and 9: " << max(4, 9) << endl;
    cout << "Average of 80, 90, 95: " << average(80, 90, 95) << endl;
    return 0;
}
`}
/>

</details>

---

## Challenge: bigger of two

Finish `bigger` so it **returns** the larger of two integers.

| a | b | Return |
|---|---|---|
| `4` | `9` | `9` |
| `10` | `3` | `10` |

<CodeExercise
  title="bigger of two"
  heading="Challenge: bigger of two"
  lang="c++"
  prompt="Implement bigger(int a, int b) and return the larger value. Hidden tests send several pairs."
  sampleLog={`(input) 4 9
9`}
  starter={`int bigger(int a, int b) {
    // TODO: return the larger number
    return a;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int a, b;
    cin >> a >> b;
    cout << bigger(a, b) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Has a function', pattern: '\\bbigger\\s*\\(', must: true, hint: 'bigger(...)' },
  ]}
  tests={[
    { name: '4 and 9 → 9', stdin: '4 9', equals: '9' },
    { name: '10 and 3 → 10', stdin: '10 3', equals: '10' },
    { name: '7 and 7 → 7', stdin: '7 7', equals: '7' },
  ]}
/>

---

## Parameters vs Arguments

| Term | Definition | Example |
|---|---|---|
| **Parameter** | Variable in the function definition | `int a, int b` in `add(int a, int b)` |
| **Argument** | Actual value passed when calling | `add(3, 7)` — here `3` and `7` are arguments |

<!-- IMAGE PLACEHOLDER: Side-by-side showing the function definition with parameters labeled, and the function call with arguments labeled, arrows connecting each argument to its matching parameter -->

---

## Default Parameter Values

You can give parameters a default value so the caller can omit them.

```cpp
void printGreeting(string name, string greeting = "Hello") {
    cout << greeting << ", " << name << "!" << endl;
}

int main() {
    printGreeting("Alice");            // Hello, Alice!
    printGreeting("Bob", "Hey");       // Hey, Bob!
    return 0;
}
```

:::note
Default parameters must come **last** in the parameter list.
:::

<details>
<summary>🧪 Default parameters</summary>

<PistonRunner
  lang="c++"
  height="240px"
  code={`#include <iostream>
#include <string>
using namespace std;

void printGreeting(string name, string greeting = "Hello") {
    cout << greeting << ", " << name << "!" << endl;
}

int main() {
    printGreeting("Alice");
    printGreeting("Bob", "Hey");
    return 0;
}
`}
/>

</details>

---

## Mini quiz — parameters

<MultipleChoice
  id="cpp-3a-params"
  title="Parameters and defaults"
  questions={[
    {
      prompt: 'In this call, 3 and 7 are…',
      code: 'add(3, 7)',
      codeLang: 'cpp',
      choices: ['parameters', 'arguments', 'return types', 'prototypes'],
      answer: 1,
      why: 'Arguments are the actual values passed at the call site.',
    },
    {
      prompt: 'A default parameter lets the caller…',
      choices: [
        'Skip that argument',
        'Change the return type',
        'Avoid declaring main',
        'Ignore type checking',
      ],
      answer: 0,
      why: 'If you omit it, the default value from the definition is used.',
    },
    {
      prompt: 'Default parameters must appear…',
      choices: [
        'Only in main',
        'At the start of the list',
        'At the end of the parameter list',
        'After the return statement',
      ],
      answer: 2,
      why: 'Trailing defaults keep the call unambiguous.',
    },
  ]}
/>

---

## Function Overloading

C++ lets you define multiple functions with the **same name** but different parameter types or counts. The compiler picks the right one.

```cpp
int add(int a, int b) {
    return a + b;
}

double add(double a, double b) {
    return a + b;
}

int add(int a, int b, int c) {
    return a + b + c;
}

int main() {
    cout << add(1, 2) << endl;         // calls int version → 3
    cout << add(1.5, 2.5) << endl;     // calls double version → 4.0
    cout << add(1, 2, 3) << endl;      // calls 3-param version → 6
    return 0;
}
```

<details>
<summary>🧪 Overloading</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

int add(int a, int b) {
    return a + b;
}

double add(double a, double b) {
    return a + b;
}

int add(int a, int b, int c) {
    return a + b + c;
}

int main() {
    cout << add(1, 2) << endl;
    cout << add(1.5, 2.5) << endl;
    cout << add(1, 2, 3) << endl;
    return 0;
}
`}
/>

</details>

---

## Scope

A variable declared inside a function only exists inside that function. It cannot be accessed from outside.

```cpp
void doSomething() {
    int localVar = 10;  // only visible inside doSomething
    cout << localVar << endl;
}

int main() {
    doSomething();
    // cout << localVar;  // ERROR: localVar not in scope here
    return 0;
}
```

<!-- IMAGE PLACEHOLDER: Visual of two function boxes (main and doSomething) with a wall between them. A variable sits inside doSomething's box with an X showing it cannot cross to main's box -->

---

## Mini quiz — overloading and scope

<MultipleChoice
  id="cpp-3a-overload-scope"
  title="Overloading and scope"
  questions={[
    {
      prompt: 'Function overloading means…',
      choices: [
        'Two functions with the same name but different parameters',
        'A function that never returns',
        'Including the same header twice',
        'Using recursion',
      ],
      answer: 0,
      why: 'The compiler picks the match from the argument types and count.',
    },
    {
      prompt: 'A variable declared inside a function is…',
      choices: [
        'Visible in every function',
        'Local to that function',
        'Always static',
        'Stored in iostream',
      ],
      answer: 1,
      why: 'Local variables exist only while that function is running.',
    },
    {
      prompt: 'Can two functions named print differ only by return type?',
      choices: [
        'Yes, always',
        'No — parameters must differ',
        'Only if they are void',
        'Only inside main',
      ],
      answer: 1,
      why: 'Overload resolution does not use return type alone.',
    },
  ]}
/>

---

## Recursion (Intro)

A function can call **itself**. This is called recursion. Every recursive function needs a **base case** that stops the recursion.

```cpp
int factorial(int n) {
    if (n == 0) return 1;       // base case
    return n * factorial(n - 1); // recursive call
}

int main() {
    cout << factorial(5) << endl;  // 120
    return 0;
}
```

How it unwinds:
```
factorial(5)
  = 5 * factorial(4)
       = 4 * factorial(3)
            = 3 * factorial(2)
                 = 2 * factorial(1)
                      = 1 * factorial(0)
                           = 1
```

<!-- IMAGE PLACEHOLDER: Call stack diagram showing factorial(5) through factorial(0), each frame stacked on top showing n and the pending multiplication, then collapsing back down with the result -->

<details>
<summary>🧪 Recursion example — Fibonacci</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
using namespace std;

int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

int main() {
    for (int i = 0; i < 10; i++) {
        cout << fibonacci(i) << " ";
    }
    cout << endl;
    return 0;
}
`}
/>

</details>

---

## Challenge: factorial

Finish `factorial` so it **returns** `n!`. Use **recursion**. `factorial(0)` is `1`.

| n | Return |
|---|---|
| `5` | `120` |
| `0` | `1` |

<CodeExercise
  title="factorial"
  heading="Challenge: factorial"
  lang="c++"
  prompt="Implement factorial(int n) with a recursive call. Hidden tests send several n values."
  sampleLog={`(input) 5
120`}
  starter={`int factorial(int n) {
    // TODO: base case, then n * factorial(n - 1)
    return 1;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    cout << factorial(n) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Calls itself', pattern: 'factorial\\s*\\(\\s*n\\s*-', must: true, hint: 'factorial(n - 1)' },
  ]}
  tests={[
    { name: '5 → 120', stdin: '5', equals: '120' },
    { name: '0 → 1', stdin: '0', equals: '1' },
    { name: '1 → 1', stdin: '1', equals: '1' },
    { name: '4 → 24', stdin: '4', equals: '24' },
  ]}
/>

---

## Function Declarations (Prototypes)

In C++, a function must be **declared before it is called**. If you define helper functions below `main()`, you need a **prototype** at the top.

```cpp
#include <iostream>
using namespace std;

// Prototype — tells the compiler the function exists
int square(int n);

int main() {
    cout << square(5) << endl;  // works because prototype is above
    return 0;
}

// Actual definition — can be below main
int square(int n) {
    return n * n;
}
```

<details>
<summary>🧪 Prototypes</summary>

<PistonRunner
  lang="c++"
  height="240px"
  code={`#include <iostream>
using namespace std;

int square(int n);

int main() {
    cout << square(5) << endl;
    return 0;
}

int square(int n) {
    return n * n;
}
`}
/>

</details>

---

## Exercises

<ExerciseSet>
<Exercise title="Number classifier" anchor="exercise-number-classifier">

:::tip Activity: Number Classifier
Write a function `classify(int n)` that returns `"positive"`, `"negative"`, or `"zero"` based on the input. Call it from `main` for several values.

<CodeExercise
  title="Number classifier"
  heading="exercise-number-classifier"
  lang="c++"
  prompt="Implement classify(int n) returning positive, negative, or zero. Hidden tests send several integers."
  sampleLog={`(input) -5
negative`}
  starter={`string classify(int n) {
    // TODO: return positive, negative, or zero
    return "zero";
}
`}
  wrapPrefix={`#include <iostream>
#include <string>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    cout << classify(n) << endl;
    return 0;
}
`}
  tests={[
    { name: '10 → positive', stdin: '10', equals: 'positive' },
    { name: '-5 → negative', stdin: '-5', equals: 'negative' },
    { name: '0 → zero', stdin: '0', equals: 'zero' },
  ]}
/>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="260px"
  code={`#include <iostream>
#include <string>
using namespace std;

string classify(int n) {
    if (n > 0) return "positive";
    if (n < 0) return "negative";
    return "zero";
}

int main() {
    cout << classify(10) << endl;
    cout << classify(-5) << endl;
    cout << classify(0) << endl;
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="Power function" anchor="exercise-power-function">

:::tip Activity: Power Function
Write a function `power(double base, int exp)` that returns `base` raised to `exp` using a loop (no `pow()` from `<cmath>`).

Expected: `power(2, 10)` → `1024`

<CodeExercise
  title="Power function"
  heading="exercise-power-function"
  lang="c++"
  prompt="Implement power(double base, int exp) with a loop. Do not use pow(). Hidden tests send several pairs."
  sampleLog={`(input) 2 10
1024`}
  starter={`double power(double base, int exp) {
    double result = 1.0;
    // TODO: multiply result by base, exp times
    return result;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    double base;
    int exp;
    cin >> base >> exp;
    cout << power(base, exp) << endl;
    return 0;
}
`}
  sourceChecks={[
    { name: 'Uses a loop', pattern: '\\b(for|while)\\s*\\(', must: true, hint: 'for or while' },
  ]}
  tests={[
    { name: '2^10 → 1024', stdin: '2 10', equals: '1024' },
    { name: '3^4 → 81', stdin: '3 4', equals: '81' },
    { name: '5^0 → 1', stdin: '5 0', equals: '1' },
  ]}
/>

<details>
<summary>💡 Hint</summary>

Start with `result = 1` and multiply by `base` exactly `exp` times in a loop.

</details>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

double power(double base, int exp) {
    double result = 1.0;
    for (int i = 0; i < exp; i++) {
        result *= base;
    }
    return result;
}

int main() {
    cout << power(2, 10) << endl;
    cout << power(3, 4) << endl;
    cout << power(1.5, 3) << endl;
    return 0;
}
`}
/>

</details>
:::

</Exercise>
<Exercise title="isPrime" anchor="exercise-isprime">

:::tip Activity: isPrime
Write a function `bool isPrime(int n)` that returns `true` if `n` is a prime number, `false` otherwise. Use it to print all primes from 2 to 50.

<CodeExercise
  title="isPrime"
  heading="exercise-isprime"
  lang="c++"
  prompt="Implement isPrime(int n). Print yes or no. Hidden tests send several n values."
  sampleLog={`(input) 7
yes`}
  starter={`bool isPrime(int n) {
    // TODO: return true if n is prime
    return false;
}
`}
  wrapPrefix={`#include <iostream>
using namespace std;

`}
  wrapSuffix={`
int main() {
    int n;
    cin >> n;
    cout << (isPrime(n) ? "yes" : "no") << endl;
    return 0;
}
`}
  tests={[
    { name: '7 is prime', stdin: '7', equals: 'yes' },
    { name: '1 is not', stdin: '1', equals: 'no' },
    { name: '9 is not', stdin: '9', equals: 'no' },
    { name: '2 is prime', stdin: '2', equals: 'yes' },
  ]}
/>

<details>
<summary>💡 Hint</summary>

A number is prime if it has no divisors other than 1 and itself. Loop from 2 to `n-1` and check if any divide evenly.

</details>

<details>
<summary>📒 Solution</summary>

<PistonRunner
  lang="c++"
  height="280px"
  code={`#include <iostream>
using namespace std;

bool isPrime(int n) {
    if (n < 2) return false;
    for (int i = 2; i < n; i++) {
        if (n % i == 0) return false;
    }
    return true;
}

int main() {
    cout << "Primes from 2 to 50: ";
    for (int i = 2; i <= 50; i++) {
        if (isPrime(i)) {
            cout << i << " ";
        }
    }
    cout << endl;
    return 0;
}
`}
/>

</details>
:::

</Exercise>
</ExerciseSet>
